# Map data notice

`china.json` and `world.json` are carried forward from the Apache ECharts 4.8 map package so the upgraded ECharts runtime can register the existing geographic views locally. They are distributed under the Apache License 2.0 in `LICENSE.apache-2.0.txt`.
