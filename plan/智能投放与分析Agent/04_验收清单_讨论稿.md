# 短链接项目：智能投放与分析 Agent 验收清单（讨论稿）

> 文档性质：第一阶段实现验收清单  
> 验收原则：每项必须有可复验证据，不接受“看起来合理”作为通过依据  
> 当前任务边界：本文档用于未来实现验收；不声明当前项目已满足  

---

## 0. 状态定义

| 状态 | 含义 |
|---|---|
| PASS | 有直接证据证明满足 |
| FAIL | 有证据证明不满足 |
| PARTIAL | 部分满足 |
| BLOCKED | 外部条件缺失且原因明确 |
| N/A | 明确不适用 |

每项验收记录至少包含：

```text
检查项编号；
状态；
证据路径或命令；
关键输出；
问题说明；
整改建议；
复验日期。
```

---

## 1. 一票否决项

任一失败，第一阶段不得验收通过。

- [ ] Agent 不能直接访问业务 MySQL 或 Redis。
- [ ] Agent 不能绕过 `admin` 用户权限访问 `gid`。
- [ ] Agent 不能执行未确认的写操作。
- [ ] Agent 不能自动删除、回收、禁用短链。
- [ ] Agent 回答中的数值指标必须来自工具返回或确定性计算。
- [ ] token、API Key、完整 IP、完整 UV 不得进入模型上下文和日志。
- [ ] Agent 不得接入短链跳转热路径调用 LLM。
- [ ] 现有短链创建、跳转、统计能力不得回归。

---

## 2. 文档验收

- [ ] `plan/智能投放与分析Agent/00_外部方案调研与讨论.md` 存在。
- [ ] `plan/智能投放与分析Agent/01_架构设计_讨论稿.md` 存在。
- [ ] `plan/智能投放与分析Agent/02_正式版开发文档_讨论稿.md` 存在。
- [ ] `plan/智能投放与分析Agent/03_技术清单与配置说明_讨论稿.md` 存在。
- [ ] `plan/智能投放与分析Agent/04_验收清单_讨论稿.md` 存在。
- [ ] 文档明确“不完全重构，先构建 Agent 再接入”。
- [ ] 文档明确安全风控 Agent 为第二阶段或预留能力。
- [ ] 文档包含外部方案参考来源。
- [ ] 文档包含待讨论事项。

---

## 3. Agent Service 验收

### 3.1 服务基础

- [ ] Agent Service 可独立启动。
- [ ] `GET /internal/short-link-agent/v1/health` 返回 OK。
- [ ] 配置不依赖硬编码 API Key。
- [ ] 服务启动失败时错误信息明确。

### 3.2 Chat API

- [ ] `POST /internal/short-link-agent/v1/chat` 能接收用户消息。
- [ ] 支持新建 session。
- [ ] 支持续接 session。
- [ ] 返回结构包含 `answer/cards/pendingActions/dataSources/warnings`。
- [ ] 无法识别意图时能追问或说明。

### 3.3 意图识别

- [ ] 能识别创建单条短链。
- [ ] 能识别批量创建短链。
- [ ] 能识别分组分析。
- [ ] 能识别单链分析。
- [ ] 能识别异常解释。
- [ ] 能识别复盘报告。
- [ ] 对删除/回收请求能拒绝并说明第一阶段不支持。

---

## 4. Admin Adapter 验收

### 4.1 权限

- [ ] 工具 API 不信任前端传入 username。
- [ ] 工具 API 使用当前登录用户上下文。
- [ ] 查询 gid 前校验该 gid 属于当前用户。
- [ ] 越权 gid 返回 403 或业务错误码。
- [ ] 未登录请求不能访问 Agent API。

### 4.2 只读工具

- [ ] `list_groups` 返回当前用户分组。
- [ ] `page_short_links` 能按 gid 查询短链。
- [ ] `get_short_link_stats` 能返回单链统计。
- [ ] `get_group_stats` 能返回分组统计。
- [ ] `get_access_records` 分页受限。
- [ ] `get_access_records` 输出已脱敏。
- [ ] 空数据场景返回空状态而不是异常。

### 4.3 写操作确认

- [ ] 创建分组生成 PendingAction。
- [ ] 创建单条短链生成 PendingAction。
- [ ] 批量创建短链生成 PendingAction。
- [ ] 未确认 PendingAction 不执行。
- [ ] 已过期 PendingAction 不执行。
- [ ] 已执行 PendingAction 不能重复执行。
- [ ] 批量创建超过上限被拒绝。
- [ ] 执行失败有错误原因和审计记录。

---

## 5. 分析能力验收

### 5.1 分组分析

- [ ] 输入“分析最近 7 天某分组表现”能调用分组统计。
- [ ] 输出包含 PV/UV/UIP。
- [ ] 输出包含趋势或日维度解读。
- [ ] 输出包含设备/地区/浏览器等至少一个画像维度。
- [ ] 输出标明数据时间范围。
- [ ] 输出标明数据来源工具。

### 5.2 单链分析

- [ ] 输入完整短链或短链描述能定位链接。
- [ ] 输出单链 PV/UV/UIP。
- [ ] 输出访问画像。
- [ ] 无法定位时追问而不是编造。

### 5.3 异常解释

- [ ] 能识别 PV 高但 UV 低的可能刷量风险。
- [ ] 能识别 Top IP 过度集中。
- [ ] 能识别设备或地区高度集中。
- [ ] 输出必须标明“可能原因”，不得给出确定性安全结论。
- [ ] 建议为低风险动作，如继续观察、拆分渠道、检查来源。

### 5.4 报告生成

- [ ] 能生成活动复盘摘要。
- [ ] 报告包含结论、关键指标、画像、异常、建议。
- [ ] 报告不包含未授权明细。
- [ ] 报告可复制或返回结构化文本。

---

## 6. 创建能力验收

### 6.1 单条创建

- [ ] 用户给出 URL 后 Agent 能生成草案。
- [ ] 缺少分组时能追问或建议创建分组。
- [ ] 能调用 `get_url_title` 辅助描述。
- [ ] 有效期缺失时使用明确默认策略并告知用户。
- [ ] 用户确认后创建成功。
- [ ] 返回完整短链。

### 6.2 批量创建

- [ ] 能根据渠道列表生成多条短链草案。
- [ ] 每条短链有清晰 `describe`。
- [ ] 超过数量上限时拒绝。
- [ ] 部分失败时返回失败项。
- [ ] 批量结果可追溯。

---

## 7. 安全验收

- [ ] 模型上下文不包含 token。
- [ ] 模型上下文不包含完整 IP。
- [ ] 模型上下文不包含完整 UV。
- [ ] URL title 被当作数据而不是系统指令。
- [ ] 用户输入中的“忽略规则”等 prompt injection 不生效。
- [ ] 内部工具 API 有鉴权。
- [ ] Agent 服务外部不可直接访问工具执行接口。
- [ ] 日志不输出密钥。

---

## 8. 回归验收

现有能力必须保持：

- [ ] 用户注册登录仍可用。
- [ ] 分组创建和列表仍可用。
- [ ] 短链创建仍可用。
- [ ] 短链跳转仍可用。
- [ ] 短链统计仍可用。
- [ ] 回收站仍可用。
- [ ] Gateway 鉴权仍可用。
- [ ] Nginx 前端代理不受影响。

---

## 9. 性能验收

- [ ] 只读分析工具单次调用 P95 小于可接受阈值。
- [ ] Agent 分析请求超时时能降级。
- [ ] Agent 服务不可用时不影响短链跳转。
- [ ] Agent 服务不可用时不影响后台传统功能。
- [ ] 批量创建有数量限制。
- [ ] 访问明细查询有分页限制。

---

## 10. 第二阶段准入条件

只有满足以下条件，才建议进入安全风控 Agent 或智能路由开发：

- [ ] 第一阶段 Agent 工具调用稳定。
- [ ] PendingAction 机制可靠。
- [ ] 权限校验经过测试。
- [ ] 回答数据来源可追溯。
- [ ] 现有短链主链路无回归。
- [ ] 用户确认安全风控 Agent 的风险等级、拦截策略和外部服务选型。
