# 风险画像与策略拦截拆分执行索引

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `11_风险画像与策略拦截实施计划.md` 拆成按交付批次执行的子计划，保证后续可以一批一测、一批一提交、一批一推送。

**Architecture:** 顶层 `11` 只保留边界、依赖和验收导航；具体 Task 细节按 gateway、agent-service 风控基础、risk-profile、risk-center/SecurityRisk Agent、admin/E2E 五个批次拆分。

**Tech Stack:** Java 17、Spring Boot 3.0.7、Spring Cloud Gateway、Spring AI Alibaba Graph、JdbcTemplate、MySQL/H2、Redis、JUnit 5、MockMvc、WebTestClient。

---

## 执行顺序

| 顺序 | 文档 | 任务 | 是否可并行 | 完成后动作 |
|---:|---|---:|---|---|
| 1 | [01_Gateway路由与策略热路径拦截.md](./01_Gateway路由与策略热路径拦截.md) | Task 1-3 | 先执行，给后续 Redis key 合同打样 | gateway 测试、提交、推送 |
| 2 | [02_agent-service风控表结构与策略发布.md](./02_agent-service风控表结构与策略发布.md) | Task 4-5 | 可在 11-03 前半段开始前完成 | agent-service 策略测试、提交、推送 |
| 3 | [03_风险画像批处理与分组聚合.md](./03_风险画像批处理与分组聚合.md) | Task 6-10 | 依赖 11-02 的通用模型和 schema | risk-profile 测试、提交、推送 |
| 4 | [04_risk-center与SecurityRiskAgent消费画像.md](./04_risk-center与SecurityRiskAgent消费画像.md) | Task 11-12 | 依赖 11-03 的画像输出 | risk-center/Graph 测试、提交、推送 |
| 5 | [05_admin正式入口与E2E验收.md](./05_admin正式入口与E2E验收.md) | Task 13-14 | 最后执行 | 三模块测试、脚本验收、提交、推送 |

## 当前执行状态

截至 2026-07-10，已完成并推送：

| 批次 | 状态 | 最新提交 | 说明 |
|---|---|---|---|
| 11-01 / Task 1-3 | 已完成 | `1acfa06 feat: enforce gateway risk policies` | gateway 路由、Redis key 工具、热路径策略拦截已落地 |
| 11-02 / Task 4-5 | 已完成 | `1140a86 feat: add risk policy persistence and redis publisher` | agent-service 风控 schema、通用模型、策略持久化与 Redis 发布已落地 |
| 11-03 / Task 6-10 | 已完成 | `f90cb24 feat: aggregate group risk profiles` | 短链画像、批处理、分组聚合与 7 天趋势已落地 |
| 11-04 / Task 11-12 | 已完成 | `bbc5bff feat: add risk center query and review api` / `58d98aa feat: let security risk agent consume risk profiles` | risk-center 查询、事件、快照、审核 API；SecurityRisk Agent 消费画像、写事件快照并自动 LIMIT_RATE 已落地 |
| 11-05 / Task 13-14 | 待执行 | - | 下一步接 admin facade 与 E2E |

继续执行时从 [05_admin正式入口与E2E验收.md](./05_admin正式入口与E2E验收.md) 的 Task 13 开始，不回滚已完成批次。

## 依赖合同

```text
Gateway 与 agent-service 共享 Redis key 合同：
risk:policy:short-link:disable:{domain}:{shortUri}
risk:policy:short-link:rate-limit:{domain}:{shortUri}
risk:policy:short-link:time-window:{domain}:{shortUri}
risk:policy:ip:block:{ipHash}
risk:rate:{domain}:{shortUri}:{ipHash}

画像与 Agent 合同：
risk-profile 只产出脱敏画像与 Top 10 异常候选。
SecurityRisk Agent 只消费画像事实，不全量扫描短链。
risk-center 查询 API 不走 Agent、不走 Tool、不走 LLM。

策略执行合同：
LIMIT_RATE 可在 HIGH + riskScore>=80 + 至少两个强 reasonCode 时自动执行。
DISABLE_SHORT_LINK、BLOCK_IP、LIMIT_TIME_WINDOW 必须人工确认。
```

## 每批固定完成动作

- [ ] 运行本批指定测试命令。
- [ ] 运行 `git diff --check`。
- [ ] 运行敏感信息扫描，确认没有真实密钥和原始 IP/user 泄漏。
- [ ] 只暂存本批文件。
- [ ] `git commit`。
- [ ] `git push`。
- [ ] 在继续下一批前确认 `git status --short --branch`。
