# 风险画像与策略拦截 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建风险画像、风险策略、Redis 热路径拦截、SecurityRisk Agent 精准诊断、人工审核与 admin 查询入口的完整后端闭环。

**Architecture:** `agent-service` 负责风险画像、风险事件、风险快照、策略落库、Redis 策略发布和 SecurityRisk Agent 编排；`gateway` 在短链跳转热路径只读 Redis 拦截，不调用 MySQL 和 agent-service；`admin` 只做面向后台页面的风险查询与审核 facade，不把风险查询封装成 Agent Tool。

**Tech Stack:** Java 17、Spring Boot 3.0.7、Spring Cloud Gateway、Spring AI Alibaba Graph、DeepSeek V4 Flash、JdbcTemplate、MySQL/H2、Redis/StringRedisTemplate、JUnit 5、WebTestClient、MockMvc。

---

## 0. 执行边界

本计划承接 `plan/安全风控Agent/10_风险画像与策略拦截增强设计.md`，不重新讨论方案选择。实施时每完成一个可测试阶段就运行本阶段测试、提交、推送。

关键约束：

```text
1. 风险画像计算不调用 LLM。
2. 风险查询 API 不走 Agent，不走 Tool，不走 LLM。
3. SecurityRisk Agent 只处理画像筛出的异常候选和组级解释。
4. Gateway 只读 Redis，不查 MySQL，不调 agent-service。
5. 原始 IP/user/visitor 不进入画像表、风险事件表、风险快照表、LLM prompt、LLM response、checkpoint。
6. DISABLE_SHORT_LINK 不修改 t_link.enable_status，只通过风控策略在 gateway 生效。
7. LIMIT_RATE 满足确定性条件时才允许自动执行；DISABLE_SHORT_LINK、BLOCK_IP、LIMIT_TIME_WINDOW 必须人工确认。
```

执行方式：

```text
每个子计划按 Task 顺序执行。
每个 Task 单独开发、测试、提交、推送。
gateway 与 agent-service 可由不同子 agent 并行开发，但同一模块同一批文件不要并发修改。
跨模块联调只在 11-05 执行。
```

## 1. 拆分后的执行文档

| 子计划 | 覆盖任务 | 交付目标 | 关键验收 |
|---|---:|---|---|
| [11-00 总控索引](./11_风险画像与策略拦截实施计划/00_总控索引.md) | 全部 | 执行顺序、依赖、状态口径 | 能作为后续任务导航 |
| [11-01 Gateway 路由与策略热路径拦截](./11_风险画像与策略拦截实施计划/01_Gateway路由与策略热路径拦截.md) | Task 1-3 | gateway dev/local 路由、RiskPolicy filter、Redis 只读拦截 | disable/block-ip/rate-limit/time-window 测试通过 |
| [11-02 agent-service 风控表结构与策略发布](./11_风险画像与策略拦截实施计划/02_agent-service风控表结构与策略发布.md) | Task 4-5 | 风控 schema、通用模型、策略落库、审计、Redis 发布 | H2 repository 与 Redis publisher 测试通过 |
| [11-03 风险画像批处理与分组聚合](./11_风险画像与策略拦截实施计划/03_风险画像批处理与分组聚合.md) | Task 6-10 | 短链画像、评分器、近 7 天活跃扫描、2 小时调度、分组画像、7 天趋势 | risk-profile 批处理与聚合测试通过 |
| [11-04 risk-center 与 SecurityRisk Agent 消费画像](./11_风险画像与策略拦截实施计划/04_risk-center与SecurityRiskAgent消费画像.md) | Task 11-12 | 风险事件、快照、审核 API；Agent 读取画像并写事件、自动 LIMIT_RATE | risk-center 与 SecurityRisk Graph 测试通过 |
| [11-05 admin 正式入口与 E2E 验收](./11_风险画像与策略拦截实施计划/05_admin正式入口与E2E验收.md) | Task 13-14 | admin facade、E2E 测试、脚本、验收清单 | gateway/agent-service/admin 全链路测试通过 |

## 2. 交付批次

```text
第一批：11-01 / Task 1 - Task 3
  交付 gateway 路由与策略热路径拦截。

第二批：11-02 / Task 4 - Task 5
  交付 agent-service 风控表结构、策略落库、Redis 发布。

第三批：11-03 / Task 6 - Task 10
  交付短链画像、分组画像、Top 10 异常候选、7 天趋势。

第四批：11-04 / Task 11 - Task 12
  交付 risk-center API 与 SecurityRisk Agent 消费画像。

第五批：11-05 / Task 13 - Task 14
  交付 admin 正式入口、E2E 闭环、验收清单与本机验证脚本。
```

## 3. 当前执行状态

截至 2026-07-10，本计划已完成第五批的代码、自动化 E2E、脚本、测试、敏感扫描、提交和推送：

```text
已完成并推送：
11-01 / Task 1-3：gateway 路由与策略热路径拦截。
11-02 / Task 4-5：agent-service 风控表结构、策略落库、Redis 发布。
11-03 / Task 6-10：短链画像、分组画像、Top 10 异常候选、7 天趋势。
11-04 / Task 11：risk-center 事件、快照、审核 API。
11-04 / Task 12：SecurityRisk Agent 消费画像并自动 LIMIT_RATE。
11-05 / Task 13：admin 风险查询与审核正式入口。
11-05 / Task 14：H2 + mock Redis + stub LLM E2E、本机脚本、run-once internal API、三模块测试与敏感扫描。

待真实环境补验：
11-05 / Task 14 Step 5：真实 DeepSeek + MySQL + Redis + admin/project/gateway/agent-service 本机闭环。
2026-07-10 探测结果：当前进程未设置 DEEPSEEK_API_KEY、AGENT_DATASOURCE_URL、AGENT_DATASOURCE_USERNAME、AGENT_DATASOURCE_PASSWORD、RISK_HASH_SALT、AGENT_INTERNAL_TOKEN；本机 8000、8010、8001、8002 端口未开放。
```

后续执行必须从拆分目录中的子文档继续，不要在本顶层文档里追加实现细节。

## 4. 总体验收命令

全部子计划完成后执行：

```bash
mvn -pl gateway test
mvn -pl agent-service test
mvn -pl admin test
rg -n "sk-[A-Za-z0-9]{16,}|DEEPSEEK_API_KEY\\s*[:=]\\s*sk-|AGENT_INTERNAL_TOKEN\\s*[:=]\\s*sk-|RISK_HASH_SALT\\s*[:=]\\s*[^}\\s]+" gateway agent-service admin plan scripts
git diff --check
git status --short --branch
```

预期：

```text
gateway/admin/agent-service 测试全部 PASS。
敏感信息扫描无真实密钥。
git diff --check 无空白错误。
工作区只包含本阶段有意修改。
```

## 5. 风险控制点

```text
1. gateway 热路径不得引入阻塞远程调用。
2. Redis key 格式必须在 gateway 与 agent-service 中保持一致。
3. hash salt 不得写入仓库。
4. 风险画像缺少某些维度时允许字段为 null，但不能阻塞画像生成。
5. riskTrend7d 只能来自历史画像或事件聚合。
6. admin 查询入口必须校验 gid 归属。
7. Agent checkpoint 只能保存脱敏后的 cards、toolExecutions、traceEvents。
8. DISABLE_SHORT_LINK 不复用回收站 enableStatus。
```
