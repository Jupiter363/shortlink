# Start with --spring.profiles.active=production --spring.config.location=classpath:application-production.properties
# Required credentials are supplied by the environment. Do not reuse test credentials.
spring.application.name=agent-service
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s
server.forward-headers-strategy=none
server.max-http-request-header-size=16KB
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=never
management.server.address=127.0.0.1
spring.jackson.deserialization.fail-on-unknown-properties=true
spring.datasource.hikari.maximum-pool-size=8
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.connection-timeout=1500
spring.datasource.hikari.data-source-properties.connectTimeout=1500
spring.datasource.hikari.data-source-properties.socketTimeout=5000
server.tomcat.threads.max=64
server.tomcat.threads.min-spare=4
server.tomcat.accept-count=32
server.tomcat.max-connections=256
server.tomcat.connection-timeout=2s
server.tomcat.max-swallow-size=8MB
spring.cloud.discovery.enabled=false
spring.cloud.nacos.discovery.enabled=false
spring.cloud.nacos.config.enabled=false
server.port=8010
management.server.port=8110
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.timeout=150ms
spring.data.redis.connect-timeout=1s
spring.datasource.url=${AGENT_DB_URL}
spring.datasource.username=${AGENT_DB_USERNAME}
spring.datasource.password=${AGENT_DB_PASSWORD}
short-link.agent.business.base-url=${ADMIN_URL:http://127.0.0.1:8002}
short-link.agent.business.internal-token=${AGENT_INTERNAL_TOKEN}
short-link.agent.business.username=${AGENT_SYSTEM_USERNAME}
short-link.agent.security.internal-token=${AGENT_INTERNAL_TOKEN}
short-link.agent.console.dev-mode=false
short-link.agent.graph.version=v07-tenant-evidence
short-link.agent.deepseek.api-key=${LLM_API_KEY}
short-link.agent.deepseek.base-url=${LLM_BASE_URL}
short-link.agent.deepseek.model=${LLM_MODEL}
# Deterministic graphs need a bounded final explanation; DeepSeek enables thinking by default.
short-link.agent.deepseek.thinking-enabled=${LLM_THINKING_ENABLED:false}

short-link.agent.risk.profile.failed-recovery-limit=${RISK_PROFILE_FAILED_RECOVERY_LIMIT:3}
