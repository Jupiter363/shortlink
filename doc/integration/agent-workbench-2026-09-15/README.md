# 双 Agent 工作台验收

关联 Issue #55；基于 main `783e3a93`，分支 `jupiter/agent-workbench-tools-20260915`。

## 交付内容

保留投放分析和安全风控的机器人、配色及三条分析方向。工具栏移至正文，加入新会话、历史分析、使用说明；提问区增加清空、复用和快捷键，输入框加高到 180px；初始卡片下增加访问统计、风险中心核验入口。完整结果可复制或导出 Markdown，历史记录通过只读抽屉查看。

历史条目保留提交时的问题和范围；失败后的保留回答明确标为上一次完整结果。复制失败提供手动复制弹窗，异步复制回调在切换 Agent、新会话或卸载后失效。运行状态的提问区和结果区均可停止前端等待，后台是否完成仍须核验。

样式限定在 Agent 页面和 `aw-*` 类，不改共享风险中心样式。正文只保留一个滚动容器并隐藏其滚动条，键盘、滚轮及触控滚动继续可用。手机分析范围独占一行。

## 自动检查

- `npm.cmd run lint`：通过。
- `npm.cmd test`：99 / 99 通过，其中新增工作台纯函数测试 9 项，覆盖只读历史、失败/运行时的旧结果标签、缺失字段、报告白名单、脱敏、证据标识、代码围栏与范围快照。
- `npm.cmd run build`：通过；最后两次布局修正后重新构建通过。
- 修改文件 Prettier 检查通过；新 CSS 由 PostCSS 解析通过。
- 独立只读复核：停止等待、全局忙碌锁、迟到响应、结果归属及历史范围未发现未解决缺陷；纯 Node 样例验证复制 Promise 在切页、重置及卸载时的归属保护。

已有 Node 模块类型提示仍存在，本轮不改 package 类型或依赖。

## 内置浏览器检查

使用 `http://127.0.0.1:5174` 已构建预览，执行真实界面操作和只读 DOM 测量。

| 场景 | 结果 |
| --- | --- |
| 两种 Agent，1226 × 986 | 原分析卡片保留；工具栏、输入区与核验入口排列正常，顶部导航无业务按钮 |
| 投放 Agent，2195 × 1080 | 工作区最大宽度 1440px，输入卡 360px，无横向溢出 |
| 风控 Agent，768 × 900 | 提问/结果切换正常，无重叠或嵌套正文滚动 |
| 两种 Agent，320 × 740 | 工具按钮正常换行，范围完整显示，卡片与入口纵向阅读，无横向溢出 |
| 手机正文键盘 End | 可到达正文底部；测得 scrollTop 与滚动范围一致 |
| 示例与清空 | 仅填入问题并聚焦输入框；清空不改变分组，无自动请求 |
| 空问题 Ctrl + Enter | 显示“请输入问题”，不发送分析请求 |
| 会话隔离 | 风控草稿及默认分组在离开返回后保留，投放页独立 |
| 核验入口 | 投放所选“内置浏览器 UAT 0914”进入访问统计后一致；风控所选“默认分组”进入风险中心后一致 |
| 说明弹窗 | 桌面不改变背景卡片几何尺寸；手机无横向溢出，Escape 可关闭 |
| 历史空态 | 桌面抽屉及手机全屏可读，关闭及返回工作区可用 |

本轮没有调用模型分析、修改风险策略或覆盖系统剪贴板。真实完整结果、运行中结果、报告下载和复制失败界面未通过线上分析触发；其逻辑由纯函数测试与代码复核覆盖。两类 Agent 的验收草稿已清理，浏览器恢复桌面尺寸并停留投放分析 Agent。
