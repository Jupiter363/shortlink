# linger10结束阶段与用户暂停收据

run `20260912T172134Z-bfc094fe`。用户要求“修复完了暂停压测，当前资源不够”后，本目录只归档已结束的smoke/6k及用户主动暂停标记，不追加任何压测。

| 阶段 | 正式计划 / 已覆盖 | 正式正确HTTP | 正式往返P99 | 原始停止原因 | 结果 / 原生退出码 |
| --- | --- | ---: | ---: | --- | --- |
| [linger10 smoke 1k](stages/linger10-smoke-r1000.json) | 10 / 10s | 10,000 | 7.846ms | 无 | PASS / 0 |
| [linger10 6k](stages/linger10-r6000-a.json) | 60 / 15.430s | 88,979 | 641.623ms | PIPELINE_FAILURE_COUNTERS_UNAVAILABLE | FAILED / **2** |
| [4k用户主动暂停](user-paused-4k.json) | 不作容量验收 | 不统计 | 不统计 | 用户资源限制暂停 | **USER_PAUSED / 不判定** |

6k全部阶段实际发送180,479个HTTP，全部正确完成，客户端/不正确HTTP均0。已调度但未发出的dropped到达3,600；另有267,421个未来到达因提前停止取消。不能把HTTP全部正确或按计划60秒分母计算的partial QPS当成容量通过。

6k停止时原始监控原因是`PIPELINE_FAILURE_COUNTERS_UNAVAILABLE`，必须保留“当时计数观测不可用”的边界，不能将缺失数据填0。之后可用端点显示EDGE rejected增量9,238、原因`queue_count`；这一事实不允许把触发停止的原因改写成另一种原因。原始`delivery`和`kafkaConservation`仍为null，accountingProblems保留`GET_DELIVERY_MULTIPLICITY_OR_UNLISTED_REQUESTS`与`KAFKA_OFFSET_MULTIPLICITY_FAILED`，不以补算覆盖FAILED。

[运行记录](runtime-and-scope.json)保留worker4、每worker sender2、linger **10ms**、每worker queue500、应用CPU0–7、依赖CPU8–11、生成器CPU12–15和构件/配置SHA。此前已有宿主竞争证据，不能仅凭此轮与旧run差异证明linger10改善、回归或硬件支持上限。

用户暂停请求时间为UTC **2026-09-12 17:31:05.811388**（北京时间9月13日01:31:05）。4k阶段由用户要求停止；归档时单次检查未发现result文件，没有轮询大JSON或等其成为容量结果，`capacityPassed=null`。不得将它计入通过/失败档位或以“稳定4k”称呼。资源清理由主线负责，本文不替代最终cleanup收据。

[source索引](sources.json)列出来源SHA。原始数百MB摘要只记录stage已有SHA和大小，不复制；完整state/env、私有配置及凭据未复制。归档未运行服务、测试或负载，原有evidence其他目录未改。文件校验见[handoff.json](handoff.json)。
