# 真实跳转按序突破执行记录

本轮承接26，按 P0 关联取证 → P1 按热点改实现 → P2 独立测试发送组织 → P3 重复平台验证执行。所有压测必须经过真实 APISIX、Java Redirect、路由/策略缓存及权威刷新，并返回真实链接的302；click、BUSINESS、EDGE三路统计保留。Agent、Analytics消费者不在本轮范围。最终OUT11在4worker/S2/L5下两次完整60秒严格通过7000/s，P99分别41.17/23.93ms；8k仍失败，已定位到同一个网关worker的上游建连超时，尚未证实底层根因或硬件极限。历史阶段及反证均保留，最终结果与资源释放见文末。

## 固定基线和判定

- 初始仍为4个APISIX worker，CPU4–7；Java CPU0–7，Redirect APC2/Xmx384m；依赖CPU8–11，客户端CPU12–15/GOMAXPROCS4。CPU均为逻辑编号，Java和APISIX的物理核心有重叠。
- sender每worker1个，batch32/64KiB、linger5ms；节点队列2000/16MiB，不扩大队列换通过；原网关限流保留。
- 64个实际TCP来源，客户端先固定2048槽。新客户端支持4096仅作为后续测量能力，不算服务优化。
- 正式10秒阶梯、60秒复测；30秒从100/s线性预热。单轮最多100万计划请求，初始整轮新增Kafka记录最多1500万（后续预算修订逐项记录于文末），按三路且最多三次发送预留磁盘，保留25GiB余量。
- 正确302和Location、P99及计划到完成P99≤80ms、零未发/取消/错误/统计失败、排空、每路生产者计数和真实Kafka分区offset匹配。失败轮保留，不用drain吞吐替代正式窗口完成吞吐。

## P0 实施

1. Java只有在实际TCP peer可信、X-Request-ID为唯一32位小写hex时复用网关关联ID；否则使用安全UUID。关联ID不承担认证和事件去重，事件自身ID继续独立。
2. 网关回传冻结ID；客户端新增全量有界关联字段和完整性计数。Java可选1/64计时从WebFilter订阅到beforeCommit，包含异步等待，排除filter前排队及响应传输，不能解释成CPU时间。
3. 诊断开关默认关闭。本轮开启后，原访问日志追加毫秒时间、worker PID、源端口、连接序号、网关/上游ID、Java采样耗时和上游connect/header耗时；外部响应不透出Java诊断头。
4. 网关发送timer延迟、出队年龄、批大小、准备/编码/connect/send/ACK/decode分段使用固定标签与有界直方图。诊断不进入每次GET完整快照编码，业务v3守恒快照不变。原始诊断文本随同一次监控抓取保存，不能放宽原监控规则。
5. 系统观察器按秒读取固定PID及startTicks对应的进程/线程、cgroup、CPU/IO/PSI；可选有界JFR，仅管理自身录制。关闭/on对照分别记录，避免把取证成本当产品瓶颈。

本轮工作目录：`.work/real-breakthrough-20260909/`。所有旧压测输入及结论保留。后续实测结果在完成后追加。

## P0 实测结果（诊断开启）

运行目录：`.work/performance/20260909T091523Z-fa0a8707/`；4个真实JAR、源码和配置已按SHA冻结，另保存16个对应源码副本。12项真实网关路径与64个TCP来源验证通过，128次HEAD确认网关覆盖伪造ID并隐藏Java诊断头。

| 阶段 | 正式窗口 | 结果 | 已发送且正确的HTTP（含预热） | 正式P99 | 统计 |
| --- | --- | --- | ---: | ---: | --- |
| p0-r1000 | 10秒 | 通过，窗口实际完成1000.2/s | 26,500 | 8.79ms | 79,500条Kafka精确核账 |
| p0-r6000-profile1 | 60秒 | 通过，窗口实际完成6000.15/s | 451,500 | 27.94ms | 1,354,500条Kafka精确核账 |
| p0-r8000-profile1 | 计划60秒，实际承压35.500265秒后保护停止 | **失败** | 405,502 | 87.21ms | EDGE拒绝560条，两路Java无失败 |

8k有1个WORKER_STOPPED发生在保护停止后的已领取任务，另195,997个计划请求取消；当时globalActive51、来源仍有31个空闲槽，没有SOURCE_BUSY证据。不能把计划60秒除出来的4733.87/s当成稳定容量。所有已完成HTTP仍已逐条按同ID匹配网关，失败没有改判通过。

6k正式有135条>80ms，其中122条集中于09:24:57.662–57.956 UTC。两条有Java采样的慢请求，Java filter订阅至beforeCommit只有0.168/0.119ms，但上游connect为28/18ms、header为73/50ms，客户端为119/92ms。这证明这些请求的主要等待在所测handler区间外；不能据此排除Netty入filter前排队。主簇所在5.878秒采样区间出现一次63.843ms linger迟到，全部215条>50ms出队等待也在该区间；同期ACK没有>50ms。

正式同窗的资源证据如下（逻辑CPU口径，不等于物理核精确容量）：

| 观测 | 6k | 8k实际压力窗 |
| --- | ---: | ---: |
| CPU0–3组平均busy | 48.92% | 59.28% |
| CPU4–7组平均busy | 74.18% | 90.63%，最高秒98.40% |
| APISIX CPU PSI some/full | 8.184% / 3.761% | 14.853% / 9.004% |
| Java Redirect CPU（100%=一逻辑核） | 216.25% | 270.13% |

容器及祖先未出现CPU节流增量，内存PSI无增量。6k发送墙钟平均仅占0.926/4槽，队列没有持续增长；不能据此将主要瓶颈改称Kafka broker容量。8k接入CPU局部压力明显增大，先做Java 0–7→0–3、APISIX维持4–7的单变量对照，比盲增sender或队列更有针对性。

JFR正式窗显示6k分配抽样约225.54MiB/s、8k约298.10MiB/s，随请求量近似线性；4个HTTP I/O线程承担约94%–95%的执行样本。6k最大GC暂停10.61ms，8k最大6.39ms，不支持用单次长GC解释全部80ms以上尾部。后续候选包括注解参数解析的对象分配、每请求观测、Host/IDN/regex、JSON和HMAC，按独立对照验证，不直接累加inclusive采样百分比估算收益。

本轮发现两项**诊断自身缺口**，原始证据全部保留：6k一次网关诊断快照仅覆盖2/4代，原v3账本仍完整；严格差分工具拒绝该流，独立审计仅跨完整同代端点计算并保留原始队列点。跨worker缓存时间是强解释，尚无逐代时间戳证明。JFR原文件包含字节完全相同的重复chunk，分析只按完整chunk SHA去重并另存派生文件；不得直接使用重复文件的总量。后续修复各自版本化，不混入CPU隔离收益。5类可能包含环境/命令行的JFR敏感事件计数均为0。

## P1 首项对照：CPU 隔离未通过，已恢复

`cpu-r8000-isolated1` 使用同一批 JVM、JAR、缓存和 APISIX，仅将4个 Java 进程所有线程的亲和性从0–7改为0–3。网关仍为4–7，总应用CPU范围仍为0–7。封装14项测试通过，实际压力期间采样、前后全部线程身份和亲和性验证通过，第一次结果写入前及finally均证实恢复0–7。

**业务失败，不能采用这个资源分配。** 含预热共发送125,439次，其中124,653次正确，632次503、154次504；正式压力仅0.916529秒，出现3,393个未发请求并取消其余计划。正式P99为1,033.17ms，不能换算为60秒稳定吞吐。三路事件失败/拒绝均无新增，但错误HTTP使正常GET三路核账不通过，失败原样保留。

隔离后末10秒预热出现明显排队，短暂正式窗GC暂停占15.63%，含38.29ms Remark；该窗口太短，不满足正式CPU差分的两个完整样本要求。它支持“缩窄Java可用核心会恶化”而非“隔离一定更快”。后续保留原CPU布局，优先验证现有函数式路由开关能否消除JFR已定位的注解参数解析分配；仍使用相同业务handler、安全过滤、租约和三路事件，并另建运行目录、统一预热。

后续运行的Kafka总预算继续从本轮最早的broker offset基线计算，包含失败轮；新进程的业务计数器可以自然从零开始，但不得重置全轮资源预算。

## P1 函数式入口对照

新运行目录为 `.work/performance/20260909T095927Z-ade55186/`，工具为 `.work/real-functional-20260909/`。四个JAR与上轮SHA完全一致，启动显式开启 `shortlink.redirect.functional-routing-enabled=true`。CPU、worker、sender、攒批和诊断配置保持原值，重新执行相同的低档预热、真实fixture创建、12项入口验证和64来源验证。

旧Redis的16个数据库都已存在历史标记，未删除或复用；新建Redis使用同一镜像ID、相同 `maxmemory=128mb/noeviction` 和CPU8–11，旧Redis停止。新端口16380/数据库1及新fixture属于运行隔离差异，应在解释跨进程对照时保留。Kafka仍为同一broker和同一topic，准入从最早offset基线累计（首次新stage前实际已新增3,026,089条），不把新run视为新的1500万额度。

新stage 28项、观察器30项纯测试通过；观察器和发生器与旧版本字节一致。`functional-r1000` 26,500次全部正确，正式10秒窗口实际完成999.9/s、P99 9.01ms，零未发/取消/统计失败，79,500条Kafka核账通过。低档结果只用于准入，不作为性能提升证据。

| 函数式6k阶段 | 实际正式窗口 | 结果 | 正式P99 | 事件结果 |
| --- | --- | --- | ---: | --- |
| functional-r6000-profile1 | 12.538611秒，保护停止 | 166,732次已发HTTP全正确，但EDGE拒绝112，失败 | 212.93ms | 原始FAIL保留 |
| functional-r6000-repeat | 完整60秒 | 窗口实际完成6000.15/s；451,499次已发全正确，1个未发，严格FAIL | 39.53ms | 无新增统计失败；1,354,497条Kafka核账通过 |

首轮JFR证实原参数解析器CHM数组分配热点消失，该数组采样权重从11.556降至0.0566MiB/s；总分配权重225.537→211.035MiB/s，约下降6.4%，不是等比例CPU或容量增益。该轮Java CPU255.17%、EDGE279.31%，CPU4–7 busy343.79%，EDGE CPU PSI some/full15.445%/7.970%，比旧6k更拥挤。非HTTP I/O差额中compiler仅5.18%、GC线程2.91%，主要来自Lettuce、change和Kafka线程，不支持把差额主要解释为JIT。

重复轮用于控制同进程波动，不改写首轮失败；也不因仅1个未发就放宽验收。接下来同JVM只将网关CPU4–7扩大为0–7，Java仍0–7，worker仍4，应用CPU并集仍0–7；包装器10项纯测试通过，实际恢复和全线程观察另验收。这里的编号是WSL内逻辑CPU，来宾拓扑与Windows宿主调度不能等同为物理核固定独占，不能据此宣称整机硬件上限。

## P1 接入调度结果与冷启动缺口

同 JVM 的网关 CPU 共享对照 `edge-cpu-r6000-shared1` 通过：完整60秒、451,500次HTTP全部正确、零未发/取消/统计失败，正式窗口5999.87/s、P99 50.34ms，Kafka 1,354,500条精确核账。Java和网关都在0–7，应用CPU并集没有扩大。相比之前重复轮，网关CPU PSI some/full从9.406%/4.454%降到2.035%/1.710%，平均和P95改善，但P99变差，不能笼统宣称延迟全面改善。

`edge-cpu-r8000-shared1` 仍失败：实际正式窗口46.976883秒，含预热497,315次HTTP全部正确，EDGE拒绝4,115条，P99 110.39ms，1个未发、104,184个取消。所有CPU设置已恢复。这次89个承压采样及前后检查中，Redirect FDSize始终2048；描述符表扩容不能解释本次8k失败。

首个函数式6k慢簇附近确有FDSize 512→1024→2048增长，后续同进程保持2048；仅能确认时间相关性，尚未证明内核扩表的RCU等待是慢簇原因。新增可选启动前FD预热实现单独验收、默认关闭；不把它算入现有JAR的稳态收益。

## P2 发送侧单变量验证准备

8k最后完整承压v3区间（10:21:09.262–15.123 UTC）新增2,030条拒绝，pending从32升至1,965、最老事件551ms。四个worker末端均有1个sender/32条在途，另外分别排队468/449/468/452条。2,555批平均17.087条，完成send平均5.54ms；send墙钟占用不是CPU利用率。最后execution诊断只覆盖3/4 worker，不能计算该段ACK、flush原因或linger差分，也不能用排空阶段补齐。

因此先试每worker sender 1→2，保持4个worker、batch32/64KiB、linger5ms和节点2000/16MiB队列；随后再依据结果试10ms攒批。网关与Java共同使用0–7。四个运行JAR和发生器字节保持不变，函数式入口保持开启；新进程/fixture/Redis数据库及以下诊断版本变化均记录，不当作严格同进程收益。

诊断修复包括：同一次抓取保留有界拒绝原因；复制各worker诊断后仅刷新一次抓取方时钟，真正未来时间仍判缺测；JFR启动不设输出文件，结束仅显式dump一次，以避免重复chunk。正式实测还须确认修复效果。网关配置/发送/快照共244项测试通过；新harness 33项、系统观察器30项通过。

继续测试的累计Kafka预算显式修订为 **2,500万条**，不是重置已有额度。始终从最早 `.work/performance/20260909T091523Z-fa0a8707/baseline-boundary.json` 计算，原SHA `1adfdb6ec074cb7623fa35eadfc7439a6692d57a33b1f1b7b5ede5312b10ef3e`，失败轮也计入。预算策略SHA `d82bf956e062a40a6183763d5c3dd9f1a58fdba1f972871a071716889fb6ca70` 固定为版本2，超过投影上限拒绝开始。重新核验WSL VHD位于本项目D盘，D盘当时可用195,367,747,584字节；每阶段仍验证物理D盘及Kafka文件系统，保留25GiB，并按每请求9条、每条4096字节预留，单阶段最多100万请求，不抵扣retention。

### FD 预热实现验收

`RedirectFileDescriptorWarmup` 和配置类已新增。`shortlink.redirect.fd-warmup.enabled` 缺省关闭；启用后 `target-slots` 缺省4096，只接受2048/4096/8192。在Netty绑定端口之前同步打开有界数量的自有 `/dev/null` 句柄，验证自身FD表容量，再全部关闭；不提高rlimit，不扫描关闭其他句柄。Linux/proc异常、余量不足、部分打开/关闭失败或最终容量不符均阻止启动。1秒是协作预算，不是可中断系统调用的硬超时。

17项测试全部通过（0跳过），覆盖真实Linux独立JVM FDSize256→4096、自有1,984句柄全部关闭及已有哨兵仍可读；低nofile1024仅限制新子进程，受控失败且父进程限制不变；真实Netty随机端口在预热完成前不能绑定，失败不能被重复启动绕过。两次独立源码复核无阻断问题。没有package/install/repackage，四个压测JAR SHA前后完全不变；此新增源码尚未进入当前压测JAR，不能记为本轮QPS收益。证据 `.work/java-fd-20260909/tests-02.json`、`handoff.md`。

### S2/L5 未通过

运行 `.work/performance/20260909T103657Z-e9885f7a/`，工具 `.work/real-batching-20260909/`；四JAR不变，Redis数据库2。1k通过，26,500次正确，窗口1000/s。两次6k均失败，不能采用S2配置：

| 阶段 | 实际正式接纳时间 | 含预热正确HTTP | EDGE拒绝 | 未发/取消 | 正式P99 |
| --- | ---: | ---: | ---: | ---: | ---: |
| s2-l5-r6000-profile1 | 1.347059743秒 | 99,515 | 522 | 68 / 351,917 | 549.94ms |
| s2-l5-r6000-repeat | 37.059030520秒 | 313,854 | 179 | 1 / 137,645 | 150.72ms |

重复轮末两段完整诊断的平均批量13.38/12.29条，仍偏小。积压段pending76→1498、8槽占满，ACK均5.78ms、P99桶上界100ms；随后队列下降但产生179条拒绝。七类原因完整、同v3守恒且差分验证后，确认179条全部为queue_count，不能推广为历史缺测轮的拒绝原因。Java FDSize全程2048、四worker1024，排除本次描述符表增长。原始FAIL、实际窗口和缺测全部保留；17份当时源码已归档，服务已停。

修复后的两份JFR各只有1个真实chunk，没有重复SHA，五类敏感事件为0且覆盖实际正式窗口。短轮Execution/Allocation/GC计数46/458/3，重复轮954/10,700/212。诊断验收通过不升级业务FAIL，短轮不能当60秒平台。诊断工具纯测试16项、JFR工具10项通过。

### 优先修正实际 Kafka 探测干扰

系统复核发现，重复轮最差约1秒（10:43:11.060–12.064 UTC）Kafka容器CPU累计2.569秒，而绑定的broker进程仅约0.58秒；不能把差额都解释成broker处理消息。原采样未含容器完整进程树，GC/fsync也缺测。随后检查**实际同一容器**，确认每10秒间隔执行一次 `kafka-topics.sh --list` 健康检查，探测运行约1.8秒且没有显式heap/APC限制。仓库compose虽已写入限制，旧容器没有应用它。这个环境差异必须控制，尚不能只凭时间和资源差异断言其造成全部拒绝。

不重建或重启broker、不重置数据/offset；对专用容器原脚本做可恢复的同目录原子替换，仅精确三个参数 `--bootstrap-server localhost:9092 --list` 时设置Xms16m/Xmx64m、APC1、SerialGC、TieredStopAtLevel1，原脚本正文逐字节保留，其他参数调用保持原环境。原SHA `bec7535636637038a6901f8ff22f49f4514e10debe2d19d752c04ddd792a37cd`；受控SHA `11caf6334e107907ccdaf18c177b46ae3c3f763116a0cc9c42f542e3bc6fcbd9`。恢复工具绑定同CID/image/initPID和当前受控SHA；正常测试收尾时恢复原文件并停资源。后续正式部署采用已有compose限制，不能把源码配置存在等同于旧容器已应用。

新增压测包装器在开始和结束分别核对脚本SHA及同一容器进程，并记录独立证据；4项测试通过，脚本正文/参数矩阵/环境及退出码透传2项测试（含6组参数）通过。修改后的实际健康检查继续成功，但墙钟仍约1.7–2.0秒，不能仅以探测耗时声称资源或QPS收益。需要同配置真实负载进一步验证。

原先S1/L10准备运行 `.work/performance/20260909T104540Z-05228fdd/` 在路径验收EP12失败后没有发压；其余11项通过，原失败保留，服务已停。失败暴露HTTP收到后其他worker的log阶段未必已发布统计，现仅对此合法、单调但不足4条的快照有界重读（最多4次、25ms间隔、总1秒）；boot变化、结构不全、回退即时失败。12个case不变，最大HTTP数显式15，16项纯测试通过。新S1/L5探测对照运行 `.work/performance/20260909T105233Z-7fa63a14/`，Redis数据库4，重新通过真实创建、路径和来源验证，原始累计预算继续使用相同起点。

## P3 编码候选的预验收

HMAC实例按调用线程复用，保持原tenant/IP长度前缀、HmacSHA256、128位截取及legacy输出。成功后复用初始密钥状态；Provider运行时异常或Error后丢弃当前线程状态。33项测试通过，包括独立fresh Mac参考、多盐/多线程交错、初始化失败及部分更新后失败恢复；四运行JAR未变，尚未计入压测收益。

JSON经标准OutputStreamWriter直接生成bytes的候选虽通过71项正确性/预算/ACK回归，但分配对照否决：常见ASCII click从1256升到9824 B/次、result从1160升到9784，中文click从3008升到9888。约8KiB的每次编码缓冲抵消了删除中间String的收益。候选已撤回并归档 `.work/p3-utf8-20260909/rejected-writer/`；不能把少一次字符串转换自动当性能提升。分配对照8.46秒、每次校验wire相等，仅代表线程分配量。

### 受控健康检查后的实测

`health-s1-l5-r6000-repeat` 完整60秒通过：含预热451,500次真实跳转全部正确，正式5999.9667/s，P99 31.393619ms，计划到完成P99 31.879969ms；零未发、取消及三路统计失败，Kafka新增1,354,500条精确核账。此前同进程首轮6k仍失败，保留冷启动差异，不能只报告重复轮。

`health-s1-l5-r8000-profile1` 失败，正式只运行约1.019140秒，含预热129,440次正确、214次未发、471,846次取消、EDGE拒绝2,292条，P99 357.34ms。前后broker身份及健康脚本均验证通过。健康检查环境修正没有证明8k突破，也没有证明该探测是所有波动的原因。

后续S1/L10运行 `.work/performance/20260909T110822Z-3289dc12/` 保持原4个JAR、4个worker和CPU0–7，使用Redis数据库5。新增只读启动预检验证实际Kafka容器及有界探测配置，相关73项纯测试通过。启动时helper曾在导入后修订异常处理分支；已在任何负载前用最终冻结版本重新执行实际预检，结果除观察时间外一致，证据 `startup-helper-revalidation.json` 明确区分启动导入版本与后续复验版本。

### Typed UTF-8 候选通过分配与语义验收

`EventJson.writeUtf8` 仅对已知Click和GatewayRequest事件使用直接字节编码；扫描显式字段，没有反射或varargs分配。含surrogate字符、未知类型及null回退原有字符串编码。两类schema的字段名和类型均由测试锁定，并覆盖全部63,488个非surrogate BMP字符；最终80项测试通过。Publisher保留原有String入口和不可变字节所有权，队列预算、重试、ACK和结算规则不变。

有界6.83秒分配实验每次同时校验wire字节一致：ASCII click 1256→856 B/次（约减少31.8%），result 1160→816（约29.7%），纯BMP中文click 2992→840（约71.9%）；emoji、截断surrogate及跨缓冲边界的回退输入分配量保持原值。证据 `.work/p3-utf8-20260909/typed-validation-final.json`。这些是分配量结果，尚不是QPS或CPU收益；新候选等待独立干净构建和真实链路复测。

### L10 首轮与观测开销缺口

`l10-r6000-profile1` 在22.722408秒预热处停止，尚未进入正式测量：53,037个302、3个504（均约1秒），2个SOURCE_BUSY未发和1个WORKER_STOPPED，其余398,457个取消。统计失败/拒绝无新增，但Kafka新增159,120条，高于3×正确302的159,111条；与网关超时后后端仍产生事件的解释相符，未逐ID证明。原HTTP失败和两项核账失败完整保留，不能把它改记为成功6k。

旧健康修正后的6k重复轮末两段完整窗口平均批量11.51/10.69条，ACK平均2.02/1.29ms、P99桶上界均25ms，pending73→68→74、无拒绝。它说明当时批量仍较小，没有证明broker硬件饱和。8k失败轮正式窗口仅约1.019秒、L10首轮正式窗口0，二者均缺少可用于稳态差分的完整端点。分析工具20项纯测试通过，绑定归档源码和原始SHA；具体证据 `.work/real-linger-fixed-20260909/edge-p2-analysis-execution-handoff.md`。

可选 `publish_execution` 在每批发送阶段编码并发布整份诊断记录（上限32KiB），自身成本尚未单独测量。后续应保留v3核账、真实Kafka和三路事件，关闭execution/HTTP timing/JFR后另测容量，明确诊断条件差异。不可将有诊断的失败档位直接称为生产或硬件上限。

`l10-r6000-repeat` 完整60秒，451,496次已发HTTP全部正确、三路事件失败/拒绝均为0、Kafka新增1,354,488条精确核账；正式窗口5999.75/s、P99 53.860265ms。唯4个 `WORKER_WINDOW_CLOSED` 导致严格FAIL：它们计划在窗口最后0.833334/0.5/0.333334/0.166667ms，实际接纳越界0.021292/0.005223/0.172649/0.176527ms；每个来源仍有31个空闲owner，全局活跃仅16–25。这是发生器严格截止的边界损失，不是SOURCE_BUSY，也不是服务器容量耗尽的证据。原PASS/FAIL规则没有放宽。

尚无足够证据把S2或L10提升为默认配置；尤其S2旧轮早于健康检查环境修正，不能从跨环境结果断言并行度2本质更差。P3组合验证先恢复S1/L5，并显式启用4096槽FD启动预热；不能把组合后的任何QPS差异单独归因给HMAC、UTF-8或FD预热。

L10末两完整采样窗合并，平均批量11.0924→17.7674条（+60.18%）、批次调用537.26→339.56/s（-36.80%）、完成send墙钟1.31157→0.928619秒/秒；但ACK均值1.648→1.926ms、排队年龄3.503→6.669ms。跨进程采样且末完整端点距截止7.23秒，缺测不补；只证明该采样区间攒批更充分，不能称端到端容量收益。

## P3 新包与真实组合验证

在全部负载停止后，以104个冻结输入文件的独立干净快照离线构建，61.59秒成功。Redirect新JAR SHA `1dfd3f0b076f8993cb87d710e803eaa520c5b7cad0c277521b0b659317ea1741`，53,194,133字节；嵌入risk-core/event-contract分别与本次reactor产物逐字节一致、必要类齐全、无废弃Writer。构建不运行测试，沿用上文独立测试记录；不复用原target缓存。证据 `.work/p3-build-20260909/build-handoff.md`。

仅在旧服务停止后替换Redirect压测JAR，原件保留于 `.work/p3-build-20260909/redirect-before-p3.jar`，其SHA仍为 `a16cb4c6fef0ea97bce92e900276037cd297bc43b64597c50d04b1a65c2bdbc6`；其他三个JAR保持原值。新运行 `.work/performance/20260909T112420Z-6c44f3e1/` 显式启用FD预热、S1/L5、4个worker、CPU与资源预算不变，Redis数据库6，诊断开启。实际FDSize和路径验收需在正式压力前再次验证。

启动前置验收全部通过：267条fixture、12项路径、64来源，以及实际Redirect进程FDSize4096和显式开关环境值。`p3-r1000` 26,500个HTTP全部正确，正式1000/s、P99 8.707581ms，零未发/取消/统计失败，Kafka 79,500条精确核账；请求ID、来源和系统采样门禁均通过。

两轮6k仍未通过，不能将局部分配优化宣传成容量提升：

| 阶段 | 实际正式接纳窗口 | 含预热正确HTTP | EDGE拒绝 | 未发/取消 | 正式P99 |
| --- | ---: | ---: | ---: | ---: | ---: |
| p3-r6000-profile1 | 7.169428679秒 | 134,149 | 1,980 | 368 / 316,983 | 565.718805ms |
| p3-r6000-repeat | 42.645012917秒 | 347,370 | 2,534 | 1 / 104,129 | 121.673530ms |

两轮Java click/result失败和拒绝均为0；停止及三路核账失败按原结果保留，不能把正确HTTP数量除以原计划60秒称为稳态吞吐。窗口取stop与scheduler的较早值（分别比scheduler值早683ns、441ns），不含排空。重复轮系统采样本身通过。原19份源已归档，服务已停后才做离线分析。

JFR检查工具仅将native全文件哈希预算从500MB改为2GiB，15项纯测试通过，JFR64MiB、事件数和时间预算保持原值。两轮JFR均只有1个真实chunk、无重复SHA、覆盖实际窗口且五类敏感事件全0；Execution/Allocation/GC分别201/2,011/32及893/12,201/184。这是诊断数据验收，不改变业务FAIL。

### P3 局部收益与剩余限制

对照健康修正后的旧JAR 6k重复轮和P3重复轮，严格取各自实际正式窗口：Java分配采样权重201.156→188.271MiB/s，GC暂停占窗2.0643%→1.0996%，但最大单次暂停9.139→11.365ms。HMAC执行样本72/1315→17/893，旧Mac初始化分配路径消失；JSON从WriterBasedJsonGenerator转为UTF8JsonGenerator，旧publisher body.getBytes路径不再采到。CPU和分配统计只支持局部优化，不是两个不同进程、不同窗口的严格因果证明。证据 `profile-analysis-findings.md`。

内部完整proc采样旧→新：Java CPU228.64%→221.91%、四个I/O线程169.95%→164.39%；EDGE283.93%→293.91%、EDGE CPU PSI some/full 2.767%/2.442%→3.308%/3.015%。没有cgroup配额节流增量，softnet drop均0，time_squeeze从7增至31。Windows物理核调度和频率不在这组proc证据中，不能解释为全机硬件饱和。

P3重复轮最后完整6.207736秒区间的2,534条拒绝全部为queue_count，其余六原因0。ACK平均2.112→5.282ms、P99桶25→100ms；出队等待3.860→66.009ms；连接阶段平均0.036→0.038ms、尾桶1ms，未见同幅放大。末端各worker pending82/299/28/15，sender均1：节点当时未满不代表此前没有局部队列满。不能仅凭ACK墙钟归因broker CPU。发送工具21项纯测试通过，原FAIL保留，证据 `edge-p2-handoff.md`。

### P4 关闭可选诊断的容量对照

运行 `.work/performance/20260909T113636Z-9a84ff43/`，工具 `.work/real-p3-off-20260909/`，相同P3 JAR、S1/L5、4个worker、FD4096和CPU布局，Redis数据库7。独立副本只移除supervisor的execution/HTTP timing开关；正式stage保留system observer，省略JFR。实际state、渲染YAML、Java环境已验证关闭，12路径及64来源重新通过。Publisher schema要求、v3、每响应ID/Location/计时、三路生产者、实际Kafka offsets和累计预算均保留；默认访问日志继续用于来源核账。独立只读审查见 `.work/p3-build-20260909/diagnostics-off-review.md`。

`off-r1000` 26,500次全部正确，正式1000/s并通过全部验收。`off-r6000` 完整60秒严格通过：451,500次全部正确、零未发/取消/三路失败，5999.9833/s，P99 **19.473148ms**，Kafka **1,354,500条**精确核账。这是关闭可选诊断组合后的结果，不能把差异只归因给某一个诊断开关。

`off-r7000` 在实际正式41.488782154秒后失败：396,921次已发HTTP全部正确，1个未发、129,578个取消，EDGE拒绝274条，Java两路失败/拒绝0；P99 76.114384ms。原拒绝和核账FAIL保持，不以短窗折算稳定QPS。19份源已归档，应用/网关及宿主补充采样进程均停止。

同一Java进程下6k→7k内部采样，Java CPU210.15%→233.31%、EDGE241.79%→264.74%；host CPU PSI7.686%→10.745%、EDGE some/full1.703%/1.472%→2.626%/2.315%。FDSize全程4096，无cgroup配额节流/softnet drop增量；openFD缺测不填零。7k末段1秒内host/edge PSI some峰值约26.637%/9.116%，表明突发调度压力仍存在。

新增有界Windows宿主补充采样，6k/7k实际正式窗内分别11/8条，PercentProcessorTime范围65–90%/64–100%，样本均值78.55%/81.625%，频率范围2958–3201/2918–3201MHz。7k的100%采样在11:48:56.082–56.527 UTC，确实早于保护截止11:48:57.529；11:49:17另一次进程观察已在导出期，不能用于解释承压根因。WMI有自身刷新/缓存和额外采样开销，每次读取约0.33–0.47秒墙钟，样本均值不能当作全窗CPU均值，频率也不能证明无热限制。证据 `.work/real-p3-off-20260909/profile-analysis-proc-report.md`、`profile-analysis-host-summary.json`。

## 8 worker 有界对照准备

继续测试已有supervisor支持的8个worker：Java与EDGE仍0–7、节点队列仍2000条/16MiB、每worker250条/2MiB，S1对应节点8个sender槽，B32/64KiB、L5和ACK不变。仅对新运行观察器适配精确8进程验收，不修改旧4worker材料；这是worker数及其派生发送并行度的组合对照，不宣称单独因素归因。

本轮累计Kafka额度显式修订为版本3、**3,000万条**（之前2,500万），用于8worker与峰值复测。原始offset基线及SHA不变，失败轮继续计入、无retention抵扣。D盘复核可用183,909,584,896字节，新增500万条按4096字节保守计约20.48GB；仍保留单阶段100万请求上限、9倍记录预留和25GiB磁盘余量。新策略仅用于尚未启动的新运行，不改旧预算文件或旧结果。

8worker观察器39项（原30+新增9）和预算12项纯测试通过，合计51项。新stage SHA `2052ad2c4123a9ff2ba820ccb91ec49a38664712ed96f00131e3d86095697fe6`，预算策略SHA `5e670aa5e20b43aab83bfb9d249bbf55633757bd8fb2dd8835503277f001ff4c`；观察器仍保留32MiB/150秒及精确进程数门槛。交接 `.work/real-eight-20260909/handoff.md`。

实际运行 `.work/performance/20260909T115443Z-b8cd8a25/`，Redis数据库8，JAR不变，FD4096、12路径、64来源及诊断关闭证明通过。`eight-r1000` 完整通过：26,500正确、P99 8.562194ms、Kafka79,500，实际8worker系统采样通过。

`eight-r6000` 首轮在约50.112386秒正式窗口后失败：392,099次已发HTTP全部正确、76个未发/59,325个取消、EDGE拒绝1,278，Java两路无失败，P99 269.893571ms。`eight-r6000-repeat` 完整60秒、451,499次已发全部正确、P99 30.000765ms，三路失败/拒绝0、Kafka1,354,497条精确核账；仅1个 `workerWindowDropped` 导致严格FAIL，busyDropped/lateDropped/workerLateDropped均0。该边界损失不能作为服务饱和证据；原FAIL不改，后续7k用于继续定位边界，不据此把6k升级为严格通过。

对新JFR中Micrometer样本另作静态复核：自定义meter与固定tag已在构造阶段缓存；采样 `timings.get(phase)` 是本地Map查询，没有发现每请求registry注册/新tag问题。暂不因采样占比直接修改或关闭指标，证据 `.work/real-eight-20260909/java-metrics-followup.md`。

`eight-r7000` 完整60秒：正式419,999次、含预热526,499次已发HTTP全部正确，正式P99 **48.594481ms**、计划到完成P99 49.022335ms，窗口实际完成6997.3/s（正式计划cohort 6999.9833/s）。三路失败/拒绝均0，Kafka **1,579,497条**精确核账。唯一失败项仍为 `noDroppedArrivals`：有1个 `workerWindowDropped`，busy/late/workerLate/workerStopped均0，未发生跨窗口开始；正式12:05:54.941505011–12:06:54.941505011 UTC。这是压测器窗口边界未发，原严格FAIL保留。8worker相较4worker的7k轮没有EDGE拒绝，属于改善迹象，尚需重复通过与更高档位确认，不将两次运行视作单变量因果证明。

`eight-r8000` 已保护停止：计划60秒，scheduler记录正式21.714896525秒，含预热293,648个正确302、15个503、1,557个未发、306,280个取消，正式P99 **375.393262ms**。三路发布器失败/拒绝增量均0；成功GET固定三路的核账门禁报形态不匹配，不能由此直接称Kafka丢消息。系统观察器正常退出且预算未丢样，但 `sufficientSamples=false`，因此本轮没有完整系统诊断通过结论。原结果工具按计划60秒输出的2871/s不能作为实际短窗或稳定吞吐使用。Java `/\{shortUri\}` 的503计数/耗时和前后事件计数正进一步做差核对；失败来源与真实stop边界以单独离线取证为准。

8k原native严格截止为 **12:11:37.749801822 UTC**，实际正式 **21.714896122秒**，比scheduler记录早403ns。内部完整样本子窗7k/8k为58.998412/21.010999秒，Java CPU233.33%→265.27%、四个I/O线程180.58%→206.17%、EDGE339.81%→383.65%；host CPU PSI19.499%→31.570%，EDGE some/full12.243%/7.304%→24.933%/14.038%。未见cgroup节流/softnet丢包，FDSize均4096，time_squeeze增量18→52。8k原系统门禁因样本不足仍为FAIL，以上仅是明确可用的正式窗子集。

Windows正式窗7k/8k分别11/4条，CPU样本均值86.455%/91.5%、范围78–97%/85–98%，频率2999–3201MHz；8k样本极少，不能把它当整窗加权均值或硬件上限。对照4worker off7k，Java CPU几乎相同233.31%→233.33%，EDGE264.74%→339.81%、host PSI10.745%→19.499%；描述性证据支持8worker调度成本上升。报告 `.work/real-eight-20260909/profile-analysis-proc-report.md`、`profile-analysis-host-summary.json`，原始失败均保留。

下一组保持节点8个sender，改为4worker/S2，CPU0–7、节点2000条/16MiB和L5不变，以区分worker增多与发送并行度的组合代价。此前S2测试早于健康检查资源修正，不能作同条件反证。本次新目录 `.work/real-parallel-off-20260909/` 继续使用版本3累计3,000万记录额度，不增加总额度。

8k完整native（514,848,326字节）流式核验确认：1,550次SOURCE_BUSY均在保护停止前，7次WORKER_STOPPED是在停止后才接纳的任务，另外306,280次取消；所以本轮未发中存在真实请求积压，不能沿用7k的窗口边界解释。15个503均为已复用连接，接收集中12:11:37.749802–37.828244 UTC，RT695.87–839.43ms。Java短链503累计count15、sum7.629911302秒；before该标签系列缺失，未擅自补零做差，具体拒绝reason没有逐ID证据。

三路实际增量为click293,648、BUSINESS293,663、EDGE293,663，共**880,974条**；Kafka click293,648、gateway587,326，合计同为880,974。它们与成功302和全部HTTP数分别吻合，支持错误响应产生两路事件的解释，原固定成功GET三路门禁仍保持FAIL。没有为了取reason新建无界topic扫描或修改消费offset。后续4worker/S2继续保留500ms请求预算和1000ms权威租约，不用增加超时或陈旧读取换容量。

OUT10工具静态/纯渲染5项通过（0.211秒），只变更lifecycle的worker8→4、显式EDGE CPU0–7、S1→S2；其余stage、观察器、prepare、预算和native二进制逐字节保持。lifecycle SHA `4749b38b8786b43d2340cb1a679e61e7cf733764369cab406d358f766f6f7913`。实际启动后必须重新完成fixture、12路径、64来源、FD4096与diagnostics-off验证，不能用纯渲染代替运行证明。OUT9的19份原源及失败分析已保留，旧Java/APISIX停止后才启动本组。

OUT10实际运行 `.work/performance/20260909T121904Z-b31eb04c/`，Redis数据库9；267fixture、12路径、64来源、FD4096、相同JAR和diagnostics-off全部通过。运行state明确4worker、S2、node8sender、每worker500条/4MiB、node2000条/16MiB，Java/EDGE/应用并集CPU0–7；依赖8–11、发生器12–15。没有用新增CPU或队列容量解释后续差异。

## 后续复核：冷启动失效重放与独立网关瓶颈

4worker/S2的 `parallel-r1000` 严格通过：26,500次正确，正式窗999.9/s、P99 8.115172ms，Kafka79,500条。`parallel-r6000` 正式实际51.430755272秒后失败：400,078个正确302、6个503、1个未发、51,415个取消；正式P99 31.794101ms，三路发布器失败/拒绝均0，系统诊断门禁通过，成功GET固定三条核账门禁仍失败。

相对旧4worker/S1 6k，Java CPU210.15%→252.71%，其中I/O160.17%→156.39%，EDGE241.79%→251.62%；额外Java成本集中在Lettuce 3.610%→21.721%、redirect-change-fanout 0.169%→18.101%。源码确认：每个新instanceId使用独立组且 `auto.offset.reset=earliest`；保留的约46.9万条路由变更会逐条触发同步Redis失效。该模式具有冷启动额外负担，但线程CPU本身不是503根因证明。

对8worker/8k阶段的15个失败ID追加了有限只读Kafka取证：两分区各8,192条、总16,384条/9,155,079键值字节，约3.214秒；15个ID全部唯一匹配REDIRECT/BUSINESS/GET/503，reason均为 **AUTHORITY_UNAVAILABLE**。位置固定在该阶段after offset以前，禁自动提交、无group订阅、前后broker/topic身份核对，自有进程已退出。这一reason同时覆盖Controller请求超时和底层权威异常，仍不能据此把具体Redis调用或500ms超时认定为唯一原因。证据 `.work/real-eight-20260909/failure-reason-result.json`。

13:05之后复测前后的专用失效组查询均为：route p0/p1 committed=end233,330/235,606，risk p0/p1均2；四分区lag0且两端相同。保持旧JAR与同进程的 `parallel-r6000-caught-up` 仍在16.272979927秒实际正式窗保护停止：187,575次已发全部正确302，1,530个未发、262,395个取消，**EDGE拒绝6,063条**，两路Java发布无失败；正式P99 504.073985ms。系统观察器正常退出但样本数不足，原严格FAIL全部保留。因本次期间没有新增失效记录，不能继续用历史重放解释全部持续瓶颈。

冷启动修复保持一般earliest/已有offset/后续分配恢复，仅首次完整分配且所有目标分区均无committed时选择固定end cut。不会把这一策略用于click或统计消费，不延长原1000ms权威租约/500ms请求预算，不清共享floor或提升全局generation；清L1不能宣称在途flight或L2已经形成强切换屏障。Kafka官方也明确指出全局latest对新增分区有跳过记录风险，故不采用一行全局替换。[Kafka 3.9消费配置](https://kafka.apache.org/39/configuration/consumer-configs/#auto.offset.reset)

## 冷启动修复验收与再次复测

`InvalidationBootstrap` 仅对首次完整且全无提交位置的分配捕获一次end cut，清本地值并定位；包含该分配的首个poll整批丢弃，再定位回同一个cut，后续新hint会被重新读取，不向前追逐新的end。已有提交、混合提交、部分分配和后续分配保持正常恢复；bootstrap失败、撤销、处理或提交失败均不宣称ready。没有为跳过的hint伪造commit。每个定位阶段有2秒预算，冷启动可能经历分配与回退两个阶段，不是整个Kafka入组生命周期硬限2秒。

最终18项unit和2项真实Kafka IT全部通过，零失败/跳过；现有两个topic共发送16条唯一hint，验证历史跳过、双实例后续广播、提交后关闭并以同group新对象补收离线期间hint。4个consumer均close完成且线程终止。真实Resolver/Clock/Sink测试确认旧flight回填仍使用原始权威时间，不续租。测试凭据 `.work/fanout-bootstrap-20260909/implementation-test-receipt.json`，SHA `4adad7864c992ba51688eca7a34e82890e18d8e4dab9c3112c66332de5c10d67`。同一对象立即stop/start的既有共享consumer竞态不在此改动中，不能把isRunning=false当作线程已退出。

新OUT11保持4worker/S2/L5、节点8sender、队列2000条/16MiB、Java/EDGE CPU0–7、依赖8–11、发生器12–15以及所有原业务门禁。另一个明确改变的测量条件是stage和系统观察器固定在CPU12–15并验收实际亲和性；停用上一轮WMI/CIM观察脚本，使用独立有界Get-Process累计CPU/PID启动时间采样。因此新旧QPS差异不能全部归因给fanout代码。宿主采样仅含进程名、身份、CPU和RSS，不采命令行或窗口内容。冻结源码24项纯测试通过，未用它们替代真实压力验收。

本轮预算版本4上调至累计3,500万Kafka记录，原始基线SHA保持 `1adfdb6ec074cb7623fa35eadfc7439a6692d57a33b1f1b7b5ede5312b10ef3e`；历史失败全计入，无retention抵扣/数据删除。磁盘复核175,820,345,344字节可用，新增500万条按4096字节预留20.48GB并保留25GiB空闲底线；单阶段100万请求和9倍消息预约不变。新策略SHA `a752b19011978f085f3dcd0bd55f57e6f085aa61296af7ef5126250d981293f5`，只应用于尚未启动的OUT11，旧预算和FAIL结果不改。

OUT11实际运行 `.work/performance/20260909T132900Z-09adb5ee/`，Redis数据库10。隔离构建106份输入对应20/20测试凭据；新Redirect JAR SHA `e805c149d0d5a414068c5768c961b9f16daeb1ad1ddf687263a0f429fb7a0f4d`，其余3个JAR不变，旧Redirect已备份。FD4096、12真实路径、64实际来源、诊断关闭及观察器CPU12–15均重新验收。独立只读组查询startup、1k/6k/8kpeak/8k/7k之后均为route233462/235749、risk6/6，committed=end、lag0；这些抽样证明进度，不把它当每条hint语义处理证据。

| 阶段 | 严格结论 | 正式请求与延迟 | 实际Kafka增量 |
| --- | --- | --- | --- |
| 1k，10秒 | PASS | 含预热26500次全部正确，窗口999.9/s | 79500 |
| 6k，60秒 | PASS | 正式360000次全部正确，窗口5998.4167/s，P99 23.753602ms | 含预热451500次，对应1354500条 |
| 8k峰值，10秒 | FAIL | 正式79999次已发全部正确，P99 40.316953ms；1个workerWindowDropped | 含预热201499次；三路失败/拒绝0，原核账通过 |
| 8k，实际59.989697869秒 | FAIL | 正式479199次已发，119个异常HTTP、801个未发，P99 59.93105ms | 固定成功GET三条形态核账失败，另作离线差分 |
| 7k，60秒 | PASS | 正式420000次全部正确，窗口6999.95/s，P99 41.168390ms | 含预热526500次，对应1579500条 |
| 7k重复，60秒 | PASS | 正式420000次全部正确，窗口6999.9167/s，P99 23.933226ms | 含预热526500次，对应1579500条 |

7k零未发/取消/HTTP错误/三路失败/拒绝，排空与Kafka实际offset通过；正式cohort7000/s与窗口实际6999.95/s分开记录，尾窗后完成不计入窗口吞吐。这是新的单次完整60秒通过档位，相对此前6k档位提高约16.7%，不能据此宣称长期平台或全部收益来自单一代码改动。8k短窗唯一失败仍保留，不将它提升为峰值通过；8k长窗出现真实失败，也不能归为只有压测器边界问题。

### 本轮可支持的归因

正式内部采样6k/8k长/7k分别：Java203.13/246.80/227.11%、四I/O线程150.27/191.10/175.76%、EDGE236.25/296.66/274.12%；host CPU PSI7.241/13.828/10.711%。新6k fanout/Lettuce为0.169/3.508%，此前冷组18.101/21.721%的额外工作已消失，与冷启动修复预期一致。该下降不是统计Kafka发送成本消失，也不证明所有旧故障都由历史重放引起。

四轮FDSize4096，无记录到的cgroup节流和softnet丢包；8k长窗最后完整差分13:41:01.725968–02.834150 UTC，Java235.91%、EDGE286.75%、host PSI11.019%，没有聚合CPU突增。直到真实截止13:41:03.439093122尚余约0.605秒未形成完整差分，故不能排除亚秒级停顿，不能从秒级平均直接确定某个504原因。证据 `.work/real-fanout-20260909/profile-analysis-summary.json`、`profile-analysis-report.md`。

Windows有界Get-Process采样在正式窗内约55.4秒的稳定PID差分显示：6k/8k长/7k非vmmem进程合计290.43/295.53/321.89%（100%表示一个逻辑CPU）。其中svchost PID3076约80%，只读tasklist确认对应Winmgmt；WmiPrvSE PID4344约60%。这是主机资源竞争证据，没有停止这些共享服务。vmmem与内部Java/EDGE不能再重复相加；进程差分遗漏中途退出/新建/不可读取计数，且不是全窗原子采样，不能当全机硬件饱和证明。观察器自身换型与绑核也是跨轮收益归因限制。

### 8k失败收敛到网关上游建连

完整785,077,114字节native经有界流式解析、全文件SHA核验：119个异常全部504，均复用下游连接，RT262.825–438.545ms。716个SOURCE_BUSY在首个504停止前发生，sourceIdle全0、globalActive1846–2048；另外85个真实分类是DISPATCH_WINDOW_CLOSED，未尝试发送，统一在90,001.294849ms作决定，晚于停止。不能把这85个归为WORKER_LATE。算术及owner归还守恒通过，但measurementComplete/scheduling_complete为false，原复合门禁及原FAIL保持。

对当前APISIX精确CID/image/PID作前后绑定，只读13:41:02–05 UTC的日志，限10秒/8MiB，实际2,650,674字节stdout及39,613字节stderr：**119条全部为 `upstream timed out ... while connecting to upstream`，全部来自容器内worker PID50**，从13:41:03.438940973到03.488935016，目标172.17.0.1:8003。读响应头超时、connect系统调用失败、无可用上游均未出现于这119条。与native504的计数及时间吻合，说明本次失败位于APISIX到Redirect的建连阶段；error日志没有native request ID，不伪称逐ID对应。证据 `edge-error-window.json`、`failure-analysis-handoff.md`。

本轮三路实际delivered为click600580、BUSINESS600580、EDGE600699，均零failed/rejected；Kafka精确增1,801,859条，与三路合计一致，也等于成功302×3再加119个EDGE事件。旧Java503的“每失败两条”模型不适用于这里。聚合无缺口不等于逐ID恰好一次，但原固定成功×3门禁失败不能被解释为Kafka丢消息。严格实际正式窗59.989697869秒内477176次正确完成，约7954.30/s，仍为失败短窗吞吐，不是通过容量。

实际nginx和APISIX balancer源码确认已启用HTTP/1.1上游复用：每worker闲置池320、复用请求上限1000、闲置60秒；pool按scheme/host/port组合，非短码或来源IP分池。320不是节点活动连接上限。当前监听8003的真实backlog为4096，空闲时RecvQ0；TCP累计ListenOverflows/ListenDrops/Syncookies均为0，四个网关worker FDSize在8k前后同PID持续1024。没有证据支持直接加大backlog、扩大队列或放宽0.2秒connect超时。119个同worker错误在约50ms排出，更值得验证其事件循环/调度暂停；1000次连接轮换仅为潜在诱因，尚无上游连接4元组和关闭原因支持，未直接改成10000。

下一项精确诊断应以该worker的有界高频schedstat/运行队列等待、loop-lag以及上游SYN/ESTABLISHED/close的同窗关系区分：CPU执行过长、被调度延迟、内核阻塞还是实际握手慢。若只看秒级平均CPU或upstream_connect_time，会把计时器处理迟到与真实握手延迟混在一起。既有keepalive下游连接不会随其他worker空闲而重新分配，因此新连接接入均衡也不能保证后续负载始终均衡。该诊断尚未执行，当前不宣称已彻底修复8k故障或达到硬件极限。

### 压测工具导出预算的独立问题

native全部请求wait完成时间约13:41:03.696379599，记录的客户端生命周期结束为13:41:29.807217，约26.110837秒用于后续汇总、JSON编码、写入/Sync及外层退出检测。stage从launch附近开始的warmup+duration+25=115秒门禁一直等到进程退出，覆盖这段后处理；没有精确SIGINT触发UTC，不把推算deadline写成实测时刻。finished日志只在Encode+Sync成功后打印，本次日志完整且流式SHA/守恒通过，不是被SIGKILL截断的JSON。

首次504和716个繁忙拒发都在导出前，工具问题不能把原FAIL改成PASS。后处理RSS峰值约5.64GB也不能当作正式业务窗口资源，不能解释更早发生的504。后续工具修订应加入绑定子进程身份的小型requests-drained标记，将原活动/排空门禁与独立有界导出门禁分开，保留每个阶段准确时间；请求deadline、尾窗、不遗漏到达、三路统计和Kafka守恒要求全部不变。此处仅完成诊断与修订设计，未修改冻结工具或预算。证据 `measurement-analysis.md`。

### 重复验收与收尾

所有独立分析计算结束后，保持同一JVM、网关worker、配置、CPU/队列预算与冻结源码执行 `fanout-r7000-repeat`。完整60秒严格PASS：正式420000次全部正确，零未发/取消/异常HTTP/三路失败或拒绝；窗口实际6999.9167/s，正式cohort7000/s，P99 23.933226ms、计划到完成P99 24.356807ms。含预热526500次，对应Kafka1579500条，三路计数、实际offset、排空、来源、系统采样及观察器亲和性全部通过。故本轮有两次60秒的7k严格通过结果，不能扩展为小时级稳定性或Agent/Analytics消费者容量结论。约10个热链接的L1/L2/权威刷新场景和HTTP范围不变，未测试TLS与生产多节点。

重复前累计实际29,223,537条，9倍阶段预约4,738,500条、预计33,962,037，低于35,000,000上限；本阶段实际新增1,579,500条，阶段after口径累计30,803,037条。原基线不变、历史失败全计入，无数据删除或retention抵扣。最后失效组查询仍为四分区lag0；OUT11共21份冻结源码已归档。

所有压力/Windows补充观察器退出后，已停止OUT11应用及网关，恢复Kafka临时健康脚本原始字节；专用Redis、MySQL/Kafka/Minio及owned Docker引擎均停止，原依赖CPU集恢复，测试数据保留。Redis停止首次因缺少显式socket环境在前置断言处拒绝，补齐专用DOCKER_HOST后停止成功；依赖收尾在Redis尚运行时拒绝最终通过，Redis停止后重新核验成功，未改默认Docker。最终11个运行的supervisor/JVM身份及资源恢复审计PASS，证据 `.work/real-eight-20260909/final-resource-audit.json`。WSL整体与共享Windows服务未关闭。

本轮新增修复的验收为18项unit+2项真实Kafka IT、隔离构建、真实路径检查及上述压力复测。宿主CPU结论以绑定最终proc报告的 `host-analysis-summary-v3.json` 为准；早先v1/v2保留为修订历史。新静态上游源码采集取得6份文件并核验容器前后身份，但一次tar后置检查退出1、未保存准确tar状态；因此报告将其标为部分采集，没有把它记成完整命令PASS。Netty四次本地javap均退出0。剩余高频调度/TCP归因与工具阶段预算修订明确列为后续工作，没有以推测改动生产参数。
