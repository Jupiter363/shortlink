# 元数据任务执行优化与复测

日期：2026-09-07；分支：`codex/production-refactor-v07`。状态：实现、单元/集成验证和两轮复测完成；8个worker独立运行11阶段全部通过，包括一次100/s、5分钟创建测量。尚未签收生产容量。

## 问题与边界

[上一轮](09-Outbox确认与元数据吞吐优化.md)同资源创建100/s时，metadata接收约100条/s，任务表内终态执行约52.78条/s，总未完成量2131触发保护。Outbox pending峰34、未入表峰14，表内pending峰2121，支持本轮优先减少worker数据库往返；没有据此扩容Kafka。

第一轮保持4个worker、4个JVM各384MiB和相同连接池/CPU/网关保护，保持已有64任务或1秒任务边界量子及50ms压测空闲轮询。生产默认1000ms仍需单独看待。Agent、统计消费者及真实外网抓取不在本轮；固定`.local`拒绝路径用于比较任务领取和终态写回成本。

## 实现方案

1. 领取先锁定任务行，再以数据库时间在条件UPDATE中判断可领取状态、到期和重试时间，同时设置新租约；检查影响行数，减少独立DB时间查询。
2. 写回合并重复围栏读取，最终条件UPDATE保留jobId/state/fence/owner/数据库实时租约条件，失败回滚整个事务。账号、组、路由的锁顺序及targetRevision/digest校验保持。
3. worker事务显式REQUIRES_NEW、READ_COMMITTED，避免等待账号锁前已建立的RR快照导致读取旧gid；计时包括commit，提交未知不计成功。新增candidates/claim/apply/retry分段耗时，success/empty/obsolete/stale/failed固定结果，无job、tenant或URL标签。

包括Group.lockActive中的一条SQL，当前正常任务候选/领取/写回应用SQL调用预期18→13；这是代码路径调用数，不等于数据库实际网络包或物理执行数。SQL对照阶段固定4个worker，保留安全检查与租约长度；其结果未提升容量，随后单独验证8个worker的并行度配置。

## 验收安排

补充旧围栏、租约到期、写回中到期整事务回滚、版本/归属变化与事务提交边界测试，统一运行Command完整单元和集成套件。之后新建独立性能运行，冻结制品和资源，准备与上轮相同夹具并完成创建/跳转冒烟、实际网关路径检查。依次运行25/50/100创建、100/500条批量、代表性网关900/s；保持2000条积压保护、300秒排空、14项失败计数门禁。失败立即停止，对未确认写请求只读逐项对账，不重试补算成功。

仅所有前置阶段通过且排空时放行100/s、30秒暖机加300秒测量；短时或单次五分钟通过均不替代生产容量确认。原始证据、失败与数据保留在本地专用测试目录，本轮结束关闭服务和依赖。

## 实际结果

完整Command验证82项单元、55项集成测试通过，0失败、错误、跳过；新增14项H2执行事务测试及6项真实MySQL/ShardingSphere集成执行。H2仅将MySQL时钟表达式替换为受控数据库时间，不冒充真实MySQL时钟；真实库覆盖租约到期、最终更新0行导致资源与任务全回滚，以及身份读取后等待账号锁期间移组并删除旧组。

验证日志`.work/verification/perf-metadata-execution-command-verify-1.log`，XML汇总`perf-metadata-execution-command-evidence.json`；Command JAR SHA256：`17dd539b01634c61d456d4276d7343416ad4cd07f60539dc1ac40542f8e5177a`。其他模块制品沿用上一轮，不声称本轮重跑其完整测试。

执行观测新增10项离线测试及4项定向兼容测试通过；严格要求固定60条唯一series，缺失/异常不补0，零样本均值为空。执行计时是补充诊断，未改变原14项失败门禁；最终额外核对每个操作的failed增量和计数单调性，stale/obsolete单独展示。

### v5：减少SQL往返的对照结果

运行`20260907T043101Z-f6e65781`，数据库`shortlink_perf_45b7384996`、Redis DB14；788份源码冻结，四JAR结束SHA均匹配。11300条夹具、142次准备请求全部成功，准备214.991秒；12项实际网关边界检查通过。冷启动12:31:34有一次Outbox领取事务超时，基线claim_failures=1并保持至结束，不清零、不计成负载期间失败。

四项冒烟创建/直连GET/HEAD/网关GET分别30/31/30/31次，全正确；P99为77.11/23/14.71/15.70ms，HEAD点击为0。原始请求123条=REDIRECT GET62+HEAD30+APISIX31，基线后SQL由11300增至11330。

| 创建档位 | 测量正确/发送 | 测量时间 | 正确吞吐 | P99 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 25/s | 2251/2251 | 完整90秒 | 25.011/s | 64.5ms | 通过 |
| 50/s | 4500/4500 | 完整90秒 | 50.000/s | 69ms | 通过 |
| 100/s | 1485/1489 | 仅14.883秒 | 短窗口99.778/s，不签收 | 短窗口80.16ms | 保护停止 |

100/s全程约44.888秒：4489次发送、4485次确认、4次保护中断未完成。只读逐请求核验4489个索引均唯一落库、路由与保存结果匹配，不补算4次HTTP成功；SQL完整性通过。总未完成峰2202（未入表峰16、表内峰2193，各峰不能相加），约30秒后观察排空。

相同活跃采样第3至11行、约40.002秒内：intake约99.97任务/s、ACK约199.94事件/s，metadata终态约50.50任务/s，未完成增长48.97/s；上一轮同类区间52.78任务/s。本次低约4.3%，单次样本不足以认定代码回退，但**没有证据声称SQL减少已提升容量**。候选查询均2.75ms、claim均32.96ms、apply均41.45ms，三段累计时间/观察时间约3.91，支持4个worker主要忙于事务操作。计时包括SQL、连接/锁等待、提交，尚不能归为具体fsync或某条SQL。Command平均约1.04逻辑核、业务池active峰12/16、pending0；候选查询约占这些累计时间的3.6%，不是当前首要优化点。

7阶段6通过、1失败；原写入波次在100/s失败后停止。剩余两批量、edge900与五分钟阶段均记录跳过，转入新并行度实验；没有在本轮继续发送这些负载。最终新增13520条=HTTP确认13516+中断已落库4；route/metadata事件各13520，click62，request123，DLQ增量0，最终队列全部清空。最终ledger为79 PASS、4 FAIL，四项均来自同4个未确认请求；新执行审计全部可用、单调且failed增量0。

结束阶段内Prometheus与SQL不是原子采集：最后after的claim/apply累计24813/24812，后置静止最终边界均为24820，与SQL任务总量24820相等。这确认先前7/8个计数差在后续样本追平，不用于宣称丢任务或篡改原快照。所有服务和依赖已停止、CPU集合恢复0–15，WSL已停止，数据及旧失败证据保留。

### v6：有界并行度对照

由4→8 worker做独立运行，CPU/heap/实际连接池16、ACK/intake及抓取上限8均保持，默认worker4暂不调整。2026-09-07后续代码复核纠正：虽然properties写连接池8，但自建`physicalDataSource`使用`setMaximumPoolSize(16)`，实际池由代码固定为16。原有固定线程池、SynchronousQueue无排队、active去重、任务量子与租约策略不变。用新增实际占满8个worker的并发/关闭测试验证这一配置；保留2000条积压门禁和执行失败核验。

没有选择跨租户批量写回：它会扩大资源锁范围以及单项过期/未知提交的恢复影响，现有数据先支持验证有界并行。实验前假设8个worker在争用不明显增加时可能接近100/s；实际对照结果列在下文，没有据线程数承诺翻倍。真实网页抓取的内存、网络与关闭过程不由本轮`.local`拒绝夹具验证。

运行`20260907T050411Z-7064020e`，独立数据库`shortlink_perf_dc202afa17`、Redis DB15；788份源码冻结，四JAR与v5相同且结束SHA再次匹配。Command PID17993，现场`/proc`确认8个metadata-worker线程。定向Runtime单元测试19项通过，0失败/错误/跳过，其中相对前述完整套件净增2项；这不是重新运行全套84项单元测试。日志和XML摘要分别为`.work/verification/perf-metadata-eight-workers-unit-1.log`、`perf-metadata-eight-workers-unit-evidence.json`，JAR仍为前述`17dd…177a`。

11300条夹具、142/142次HTTP准备请求全部成功，用时202.439秒；12项网关边界检查通过。四项冒烟各31次全部正确，P99分别76.8/33.3/16/15ms；HEAD点击0。原始请求124条=REDIRECT GET62+HEAD31+APISIX31，基线后SQL11300→11331。启动前置阶段Outbox claim超时计数1，基线至最终保持1，负载期间无增长。

| 场景 | 正式测量时间 | 正确/发送请求 | 正确吞吐 | 往返P99 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 单条创建25/s、20账号 | 90秒 | 2251/2251 | 25.011/s | 77.5ms | 通过 |
| 单条创建50/s、20账号 | 90秒 | 4501/4501 | 50.011/s | 77ms | 通过 |
| 单条创建100/s、20账号 | 90秒 | 9001/9001 | 100.011/s | 202ms | 通过，未完成峰703 |
| 每2秒一批100条 | 90秒 | 46/46批 | 51.111行/s | 122.15ms | 通过，未完成峰755 |
| 每10秒一批500条 | 90秒 | 10/10批 | 55.556行/s | 189.12ms | 通过，未完成峰1287 |
| 网关hot10 GET 900/s | 90秒 | 81000/81000 | 900/s | 7ms | 通过 |
| 单条创建100/s、持续观察 | 300秒 | 30001/30001 | 100.003/s | 126ms | 通过 |

表中请求和吞吐仅取正式测量；批量实际边界请求使行速率略高于目标50行/s，不能写成固定50实测值，批量P99样本分别仅46和10。含30秒暖机，各创建档新增3001/6001/12001行，两批量新增6100/6500行，持续观察新增33001行。网关含暖机108000次成功跳转，click、REDIRECT结果、EDGE交付各108000。所有阶段无429、503、客户端错误、dropped或未确认请求。

同早期活跃采样L3→L11约40秒，4→8 worker使metadata终态速度50.497→96.694任务/s，约提升91.5%；intake约100任务/s、Outbox ACK约200事件/s均跟得上。claim均时32.96→35.20ms、apply41.45→43.77ms，吞吐提升来自并行度，单次事务没有变快。

90秒创建100档完整活跃采样L3→L26约115秒（包含部分暖机）终态94.172任务/s，输入100.085行/s；接近测量的L8→L26未完成93→703，最后30秒增长约9.23/s。因此这档短时通过不等于后台稳态平衡。Command平均CPU约单核的129.85%，业务池active峰16/16、pending峰1但timeout增量0；数据库事务等待与连接余量仍需关注，不能归为Kafka broker满载。

随后在同一进程、两批量和网关均通过并排空后，放行一次100/s、30秒暖机+300秒测量。33001次全程请求均确认成功，未触发2000积压或180秒持续增长保护，首个after样本已排空。未完成峰306、最老待处理年龄峰3074ms。近测量采样L8→L68为300.000067秒，输入100.000、intake100.003、ACK200.060事件/s、metadata终态100.017任务/s；未完成29→24。前150秒终态98.156/s、未完成29→306，后150秒101.877/s、306→24，末60秒101.970/s、125→24，证明本次后半程追回了积压。最后running仍24，约1.814秒后的首个after为0，这是采样边界而不是精确排空耗时。

持续档近测量窗口claim均33.79ms、apply42.38ms，Command CPU均为单核的118.27%；业务池active峰16/16、pending峰9，timeout增量0。Outbox SQL pending峰42，ACK/send队列采样峰0，69条样本执行series完整，无观察超时。连接池短时等待仍存在；pending没有触发超时不等于没有争用，也没有足够余量证据支持继续增加worker。

同为8个worker，先前短90秒档近测量终态约93.21/s，本次最初90秒已约98.83/s，两个阶段从早段就不同。缓存、锁竞争、调度和运行时预热的贡献无法由现有观测拆分，不能把改善直接归为测试时长、某条SQL或JIT；本次只证明此固定资源与受控目标下一个完整5分钟窗口可跟上100/s输入，仍不等于重复长时或生产峰值容量。

## 最终核验与运行配置

v6最终11阶段全部通过。数据库最终77935行，基线11300，新增66635=全部HTTP确认新增行；route与metadata事件各66635、click108062、request216124、metadata DLQ增量0。最终ledger **83 PASS、0 FAIL、0 N/A**；原14项失败计数没有新增，额外执行审计全部60条series完整、单调且4项failed增量0，所有队列排空。v5原失败与4个未确认响应仍完整保留，未被v6通过覆盖。

v6原`fixture-summary.json`中metadataWorkers=4是准备脚本的摘要硬编码错误；实际manifest和线程核验都是8。保留原摘要及SHA，在`fixture-summary-erratum.json`记录勘误。全部负载结束后，将准备脚本改为读取`state.resourceProfile.metadataWorkers`，四个相关Python模块语法检查通过。这个修正只影响后续摘要，不影响本轮服务配置、数据或已冻结运行结果。

本轮通过的性能配置由`supervisor.py --metadata-workers 8`显式选择，worker默认参数仍4。部署选择相同并行度时，有效覆盖项是`shortlink.metadata.workers=8`及本次诊断使用的`shortlink.metadata.poll-millis=50`；连接池16在v5、v6两轮均相同，不是第二轮新增资源。后续复核发现`CommandDataSourceConfiguration.physicalDataSource()`直接设置最大16，`spring.datasource.hikari.maximum-pool-size`不控制这个自建池；此前将properties中的8称为“实际生产默认池大小”不准确，现予更正。默认worker4与空闲轮询1000ms未直接改为性能档，物理池配置化是单独的后续改进项。

元数据正常网页抓取、访问统计消费、Agent、TLS、多机/RF3、故障下恢复容量以及重复长时容量验收均不由本次证据覆盖。当前改进解决了本机诊断档位中4个worker明显跟不上100/s输入的问题；数据库事务的绝对耗时、热点账号批量积压和资源配比仍是后续重点，不应据此承诺10万QPS。

两轮JVM、临时APISIX及4个测试依赖均已停止，依赖CPU集合恢复0–15，WSL停止；数据库、Kafka容器及原始证据保留。v6关键证据在`.work/performance/20260907T050411Z-7064020e/`：`run-manifest.json`、各`stages/`、`metadata-execution-audit.json`、`optimization-ledger-verification.json`、`final-artifact-verification.json`、`dependency-cleanup.json`、`environment-cleanup.json`。
