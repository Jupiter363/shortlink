# 硬件边界与架构、实现瓶颈的归因复核

日期：2026-09-09。本轮只读已有压测原始数据和当前源码，独立重算逐请求时间线、CPU、内存与账本调用次数；没有启动服务或新增压力流量，也没有修改产品代码、历史原始数据或验收结果。

**当前判断：软件的并行方式、连接接入调度和消息攒批组织仍有优化空间；测试设计也放大了失败现象。没有测出整台硬件的稳定上限，不能据此认定应该先扩机器或 Kafka broker。** 单 worker 接近一个逻辑 CPU 满载是真实的局部约束；它与“整机到顶”“预热后的单 worker 吞吐上限”是不同结论。

| 用户关心的问题 | 判断 | 最有区分力的证据 |
|---|---|---|
| 是否达到硬件支持上限 | 整机上限未证实；单工作进程在高档接近一个逻辑 CPU 的执行预算 | 同一网关 cpuset 下，1→4 worker 组合配置使窗口完成 QPS 从4556.60提高到5926.25 |
| 是否架构还能优化 | 可以，已有明确的执行耦合和批量分散成本 | HTTP与Kafka编码、发送回调共用worker；各worker独立攒批，4worker平均每次Produce仅2.64条 |
| 是否细节没做好 | 有具体待改善项；部分失败还来自压测模型与口径 | 新连接长期占用固定源IP连接槽；4worker每事件约2.04次完整账本编码/写回；冷接入与持续吞吐混在短窗中 |

## 1. 新发现：单 worker 的冷连接影响持续到了整轮结束

重新计算每条首次请求的开始、结束和所属owner，不再把“首秒以后”自动视为“已经进入稳态”。单 worker、目标6000/s、持续5秒的 `peak` 组有512条首次请求，它们在168.62ms内全部开始，但最后一条到 **5.106秒** 才完成。

| 距测试起点 | 仍停留在首次HTTP请求的owner数 | 占512个owner |
|---|---:|---:|
| 1秒 | 340 | 66.4% |
| 2秒 | 285 | 55.7% |
| 3秒 | 240 | 46.9% |
| 4秒 | 202 | 39.5% |

将这些首次请求与正式5秒窗口求交后积分，平均有 **264.93个owner** 在处理首次请求，即 **51.74%的总owner时间**。这是占用发生器连接槽的时间，不能换算成CPU占比，也不代表一半QPS损失可被直接追回。

因此，报告21中“首秒后仍只有约4.6k–4.9k/s”能说明问题一直存在，却不能证明冷连接影响已经结束。**4556.60 QPS是这一组冷连接排队、固定连接槽和单worker忙碌共同作用下的实际结果，不是已经测出的预热后单worker上限。** 网关首秒后99.11%的CPU仍是真实证据，但单靠它不能确定哪个原生热点导致接入延迟。

发生器源代码进一步解释了影响如何延续：64个源IP各固定8个owner；请求按计划序号选择源IP，该源的8个槽都忙时直接计busyDropped，不向其他源借槽。首次请求长时间不结束，就会占住该源一部分槽，之后计划请求继续被丢弃。

`peak` 的6796个未发请求中，6793是源池忙，只有3个是worker启动迟到；dispatcher迟到丢弃为0。`busyDropped` 并不等价于全部512条HTTP都在途，更不是服务端丢包、HTTP错误或Kafka丢消息。缺失请求没有逐条dispatch时间；利用相邻已发请求夹住尝试时间，可找到4606个缺失arrival，其来源在整个区间内都有8条HTTP在途，而全局已记录的dispatch至HTTP结束区间最多408–511条。HTTP结束到owner归还idle之间未被单独记录，不能由此声称其他源有确定数量的空闲owner。

证据：[主代理独立重算](../../../../../.work/gateway-cause-classification-20260909/root-recheck.json)、[发生器机制与分源重建](../../../../../.work/gateway-cause-classification-20260909/generator-analysis.md)。

## 2. 四 worker 的剩余失败主要集中在接入启动阶段

同一cpuset的四worker组，全部512条首次请求在 **363.62ms** 内完成；291个未发请求对应的计划时刻全部落在 **56.33–216.67ms**，其中287个源池忙、4个worker启动迟到。全部299个HTTP耗时超过80ms的样本，也都属于首秒计划到达的请求，其中215个为首次请求、84个为复用请求。

后面三个完整的计划秒，各6000个请求全部发出，HTTP P99分别为 **6.459、3.328、2.666ms**。按真正完成时刻分桶，这三秒分别完成6000、6002、5998个请求。全4秒仍有291个未发请求，HTTP P99为90.011ms，scheduled-to-completion P99为127.337ms；原容量FAIL保留。

这说明本档需要优先解释首批连接与启动调度。它不支持“稳定处理6000/s时一直压不动”的描述，也不支持宣布长期6000/s已经验收通过。最高输入本来就是6000/s，即使短时间完全跟上，也只是跟上该档输入，尚未探索更高档位。

此前逐请求客户端/服务端关联已经支持相当一部分新连接等待发生在HTTP请求计时前；但未记录精确内核accept与首次read边界。调整multi_accept只改变了尾部形状，组合组仍失败。不能把接入长尾直接归为Kafka ACK、GC或一个确定的Lua函数。

## 3. 硬件判断必须使用物理核心与逻辑 CPU 的正确口径

这台共享宿主的历史盘点为 Ryzen 7 5800H，8个物理核心、16个逻辑处理器。历史WSL拓扑显示0/1同core0、2/3同core1，8/9同core4、10/11同core5。若拓扑保持不变，网关0–3和Kafka8–11各覆盖 **2个物理核心的4个SMT线程**，不是各4个独立物理核。

这份拓扑记录早于目标压测数小时；各run没有重新保存在线拓扑。因此不能将历史映射冒充同步实测，但已经足以指出此前用“4核”泛指cpuset四个编号的表述不严谨。下次运行应冻结实时CPU/core/thread_siblings关系。

| 同组正式窗内部采样 | 单worker高档 | 四worker |
|---|---:|---:|
| 网关平均CPU，100%=一个逻辑CPU时间 | 96.67% | 258.19% |
| Kafka平均CPU，同一采样窗 | 50.79% | 304.29% |
| 网关内存采样峰 | 见原始重算 | 91.38MiB / 1GiB限制 |
| Kafka内存采样峰 | 见原始重算 | 374.98MiB / 1GiB限制 |

四worker组约100ms采样片段中，网关、Kafka最高均接近394%，说明短时执行压力确实存在。所有保存样本未见容器自身CFS节流；创建命令没有显式CPU quota，但未完整记录祖先quota与宿主调度。

**258%/400%不能推出“还剩35%算力”，304%也不能推出Kafka已经消耗3个独立物理核。** SMT、共享宿主、频率和虚拟化调度会影响实际处理能力。目前既没有容器内存打满的样本，也缺同窗宿主利用率、原生线程on/off-CPU和磁盘等待等证据，尚不能确定整机硬件天花板。

历史128GiB/4096B记录预算是测试数据保留模型，不是实测broker存储容量瓶颈；不能把预算接近用完当作磁盘或Kafka已经跑满。

证据：[CPU/内存独立重算及拓扑时间边界](../../../../../.work/gateway-cause-classification-20260909/hardware-analysis.md)。

## 4. 架构与实现层仍可削减的工作

### HTTP与事件投递仍共享执行机会

异步Kafka发送允许等待ACK时让出协程，但事件JSON、Kafka组包、ACK解析和记账仍在APISIX worker内执行。四个sender是四个异步发送槽，不是四个CPU线程。log阶段也不是让当前HTTP同步等待Kafka ACK；它的工作会影响同进程接下来处理的连接与请求。

### 多 worker 将小流量分散到独立的攒批队列

6000事件/s均匀分到四worker，每个约1500/s。1ms内约新增1.5条，加队首通常是2–3条的数量级；队首之后等31条以凑满32，按均匀到达近似需要20.67ms。**batch_size=32只是上限，与1ms到期发送组合时，并不意味着每次发32条。**

实际四worker组23709条事件产生8979次Produce，平均2.64条；Kafka平均CPU达到304.29%。单worker高档平均27.96条/Produce，可能依赖排队积累得到大批，不能因批量更大就称整体更好。前一次1ms调整减少小RPC的收益是真的，但它没有消除新连接长尾；增worker又把节点sender从4增至16，使发送组织一并变化。

### 完整观测账本仍频繁编码和发布

四worker组从准备后到最终排空，23709个事件触发48249次完整worker账本JSON编码/共享字典写回，约 **2.04次/事件**，另有每事件自身的业务JSON编码。这是具体的成本削减候选；已采到的单次短调用、少量LuaJIT采样仍不足以证明它就是百毫秒长尾或唯一CPU热点。

代码已经复用Kafka连接池，不是每条消息都获取metadata，也不是短码发号器在这条组件链路上大量计算hash。下游统计消费者没有参加本轮，不能用这些数据判断消费者跟不上。

证据：[实现审查、行号和批量数学](../../../../../.work/gateway-cause-classification-20260909/implementation-analysis.md)。

## 5. 应按这个顺序突破

1. **先修正诊断设计。** 将业务预热后的持续吞吐、冷连接突发分别设为有明确门槛的场景；两者都保留，不能从旧结果删首秒宣布通过。记录每源槽占用与实际丢弃原因，确保发生器能提供目标负载；放宽发生器槽数只能帮助测量，不能冒充网关优化。
2. **测接入为何迟迟没有执行。** 同一故障窗口记录accept/首次read、事件循环延迟和原生on/off-CPU栈。已有约44%的LuaJIT采样权重无法解析为具体C热点，继续调通用参数不能替代这一步。
3. **在固定worker下优化发送组织。** 保持CPU、节点队列、输入与时长不变，先对照每worker发送槽4→1或2，再独立调整linger；观察Produce/事件、ACK延迟、队列年龄和HTTP尾部。少槽若只增加积压就撤回，不能为了批量数字牺牲交付。
4. **按实际热点优化账本与编码。** 优先减少反复构造的身份/字段与编码开销；需要降低发布频率时，必须保留崩溃、未确认事件和最终排空的可见性。不能直接删统计、降ACK要求来换QPS。
5. **再决定架构隔离或加资源。** 如果事件处理仍明显挤占HTTP执行，可评估有界的节点聚合/独立发送进程，算上IPC和故障处理成本，避免产生新的单进程瓶颈。只有可重复对照证明分配的物理计算或IO资源持续饱和、加资源能提高有效吞吐，才把它归为需要扩容的硬件约束。

当前优先级是**软件调度、发送组织与测量细节**；不是已证明需要推翻Kafka方案，也不是已证明买更多机器就能解决。冷连接长尾的唯一底层函数尚未定位，完整短链业务的稳定QPS上限仍未知。

## 验证范围

本轮主代理对baseline、peak、workers4、workers4acceptretry四组原始逐请求数据重新计算，并校验计划数=已发数+未发数、已发=正确响应、每组512个首次连接及原capacityPassed=false；与独立分析结果一致。CPU/内存另从原始cgroup样本重算。当前没有新产品改动，未重复运行单元/集成测试。

全部结论仍仅针对 **APISIX → 固定302桩，同时向Kafka投递事件**。Java Redirect、生产限流路由、统计消费者和Agent未参加这些组件压测；不能把本结论写为完整系统容量验收。
