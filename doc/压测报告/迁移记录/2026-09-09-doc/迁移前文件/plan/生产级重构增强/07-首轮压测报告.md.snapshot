# 首轮接口与网关跳转压测报告

日期：2026-09-06 至 2026-09-07。分支：`codex/production-refactor-v07`。

**状态：首轮诊断完成，已按停止条件终止上探；尚未进入高QPS与长时容量验收。** 已完成接口短探索、跳转冒烟和真实批量号段切换，发现路由证明过期与Outbox发布积压两个阻断问题。

运行标识：`20260906T151359Z-4f3e05aa`；原始证据位于 `.work/performance/20260906T151359Z-4f3e05aa/`。不修改被测 Java 业务制品，不把失败轮覆盖成通过。结果只适用于下述本机配置，不能换算为正式集群容量。

## 1. 已确认的阻断问题

### 1.1 正常跳转在每秒一次时出现路由证明过期

同一有效短链、固定访客身份、每秒一次、30 秒校验，结果如下。k6 在时间边界实际各发送了 31 次请求，报告使用真实次数。

| 场景 | 实际发送 | 正确 302 | 503 | p99 完整调用耗时 |
| --- | ---: | ---: | ---: | ---: |
| Redirect 直连 GET | 31 | 28 | 3 | 32.1ms |
| Redirect 直连 HEAD | 31 | 25 | 6 | 26.6ms |
| APISIX GET | 31 | 29 | 2 | 22.1ms |

有限读取两分区共 124 条请求原始事件，确认上述 **11 次 503 全部是 `ROUTE_PROOF_EXPIRED`**。APISIX 的两次 503 是转发上游响应，无 HTTP 429。未观察到错误 Location、HEAD 点击或生产者 failed/rejected 增量。原始事件只导出状态与原因计数，不导出访客内容。

`RouteResolver`、`RedisRouteCache` 只检查缓存当下是否有效，允许返回只剩极短有效期的路由。后续策略读取、评估或限流完成时，`RedirectController` 的最终检查发现证明已经过期，于是拒绝跳转。最终检查保留了安全约束，但缓存读取没有为后续处理留下时间预算。

对应代码：`shortlink-redirect/.../cache/RouteResolver.java:60`、`cache/RedisRouteCache.java:68`、`web/RedirectController.java:96–135`。现有固定时钟或距离到期约500ms的测试未覆盖“缓存剩1ms，处理过程中越过边界”。

建议修复：在路由与策略缓存的读取入口校验剩余证明预算，不足时通过既有请求合并及回源限额读取权威事实，保留最终有效期检查。不能改写时间戳、延长1秒证明或删除最终校验。若采用受限刷新重试，应放在限流扣额之前，并重新绑定路由、目标和策略，避免重复扣额或沿用旧上下文。

本轮因此停止跳转上探。上述延迟不能用来计算网关开销；三组成功条件未达标。正式热集/大工作集跳转、900/s基线、超额保护及长时稳态均不能据此签收。

证据：`smoke-event-reasons.json`、`stages/smoke-direct/`、`stages/smoke-head/`、`stages/smoke-edge/`。

### 1.2 创建返回较快，Outbox 发布链路先积压

单账号1、5、10请求/秒的短探索通过，测量段成功率均为100%，p99分别约49.2、44.0、58.0ms。**这只是30秒预热＋90秒测量结果，尚非三轮长时容量确认。**

25请求/秒档的Outbox采样峰值达到2064条，触发预先固定的2000条停止门槛；停止输入后约105秒排空。该档metadata采样峰值为4，Command业务池活跃峰值3/16、等待峰值0，没有连接池耗尽证据。

`OutboxPublisher` 每轮固定领取8条，逐条等待 Kafka ACK 并执行 ACK SQL；整轮结束后再等待250ms。每条新链至少产生route、metadata两条事件。因此单发布器的持续理论上限为：

`新链/秒 ≤ 8 ÷ [2 × (0.25秒 + 领取耗时 + 8条串行发送确认/ACK耗时)] < 16`

这个上界描述“Outbox长期不增长的可持续创建速率”，来源于源码推导；接口短时仍能接受更高输入并留下积压。预装每500条链接实际约39.8–52.7秒排空，与此数量级一致。调度池4线程不会让同一个 `fixedDelay` 方法并发执行四份；把metadata轮询改为50ms也无法越过上游发布上限。

建议优先优化有界领取批次与有限并行发送/确认，保留事件ID、租约围栏和逐条成功/失败记账；结合积压增加创建侧准入和告警，再验证重试、接管、乱序及排空。不能只上调HTTP入口限额。

对应代码：`shortlink-command/.../outbox/OutboxPublisher.java:73–110`、`link/LinkCommandService.java:247`。

25/s被停止时，全轮实际发送1997次、完成1994次、明确正确1993次，数据库新增1996行。日志有1次发生在停止前的EOF；停止时另有3次调用未完成。只读核对所有命令结果、路由、唯一ID和各租户额度，找到1996个唯一提交，发送序号1145未落库，无额外序号或错账。**数据库提交不补计为HTTP成功，原始失败轮保持失败。** 缺少逐请求响应日志，不能确切归因每个未确认结果。

k6 2.2.0 的运行中 `constant-arrival-rate` 不支持暂停；SIGINT会取消正在运行的请求，正常时长结束时的 `gracefulStop` 与之不同。本轮没有用另一种负载模型伪装固定到达率。证据：`stages/a1-c1-r25/reconciliation.json`、`k6-interruption-notes.md`。

### 1.3 standalone 两路限流 key 存在共享风险

当前跳转 `limit-req=1000/s, burst=200`，并发 `limit-conn=200, burst=100, default_conn_delay=0.1`；管理漏桶为100/s、burst100，并发值相同。key为 `remote_addr`，按来源IP计量，节点内worker共享额度，不能乘worker数或视为集群总并发300。

实际镜像源码的 key 拼接使用 `remote_addr`、配置类型和配置版本；standalone同文件的路由版本相同，管理与跳转可能使用相同key。本轮纯路径测试避免了两路混压，此发现来自源码分析，尚未做跨路由HTTP竞争复现。

建议给两路的 `limit-req` 和 `limit-conn` 均配置 `var_combination` 与明确的路由前缀，再进行跨路由隔离回归。未在本轮修改生产限流参数。证据：`edge-limiter-source-notes.md`。

## 2. 本轮配置与证据口径

- 本机独立WSL，16逻辑CPU、约16GiB内存。四个Java进程各Xmx384MiB、ActiveProcessorCount=2，cpuset 0–7；APISIX两worker在4–7，与Java存在交集；依赖8–11，k6在12–15。ActiveProcessorCount不是CPU硬配额。
- MySQL buffer pool256MiB，Redis128MiB/noeviction，Kafka512MiB堆、两分区、RF1。精确镜像、JDK/k6版本、制品SHA和源码快照在 `run-manifest.json` 与 `source-checksums.json`。
- 创建路径：压测器→Admin8002→Command8001，保留内部身份校验；直连跳转从可信APISIX网络空间访问Redirect8003；网关跳转在同网络空间访问APISIX9080。归一化访客IP相同。公开跳转不经过Java Gateway。
- HTTP/1.1 keep-alive，不跟随302，固定Cookie、UA、Referer与随机种子；新建幂等键按阶段和迭代唯一。创建同时检查HTTP及业务结果，批量核对全部行。
- 通过真实接口准备20账号、10000普通链接、1280变更链接、20幂等种子，共11300行。142/142准备请求成功，12项真实边界接线检查通过；私有fixture权限0600。
- 元数据目标使用明确的 `.local` 拒绝路径；poll50ms与默认1000ms不同。FAILED是该受控路径的预期结果，不代表正常网页抓取通过。
- 最大新增50万行；持久任务pending达到2000停止输入，5分钟内观察排空。采样每5秒，所报峰值是采样峰值。Kafka生产真实开启；不测试Agent、LLM、Flink/ClickHouse消费及统计查询能力。

## 3. 接口矩阵与通过口径

共完成23个阶段：16个通过阶段检查，7个失败。其中4个是30秒契约冒烟；其余为30秒预热＋90秒测量的短探索，触发保护时提前结束。**没有取得可靠容量结论。** 每阶段原始结果在 `stages/`；完整吞吐、p50/p95/p99、错误、事件、CPU、GC、积压及发号增量见 `results-summary.json`、`results-table.md`。

| 场景 | 已执行目标率 | 结果与测量段 p99 |
| --- | --- | --- |
| 单账号新建 | 1、5、10请求/秒 | 短探索通过；49.2、44.0、58.0ms |
| 20账号新建 | 1、5、10请求/秒 | 短探索通过；38.4、89.0、52.0ms |
| 新建上探，两种账号配置分别运行 | 25请求/秒 | 均触发Outbox 2000条停止门槛；提前结束，不进入50/100档 |
| 20账号幂等重放 | 1、5、10、25、50请求/秒 | 全部通过，p99 12–14ms；50档正确吞吐50请求/秒；新增行、额度和已发ID增量均为0 |
| 10条同步批量 | 0.1、0.5、1批/秒 | 全部通过；50.9、50.6、36.5ms；1批/秒档实际正确产出约10.1行/秒 |
| 100条同步批量 | 0.1批/秒 | 通过；正确产出10行/秒，p99 70.7ms |
| 100条同步批量 | 0.5批/秒 | 预热即停止，15批/1500行全部返回成功并落库；未进入测量段，p99与吞吐N/A |
| 500条同步批量 | 0.1批/秒 | 预热即停止，3批/1500行全部返回成功并落库；未进入测量段，p99与吞吐N/A |

低频批量的测量段只有9或10个请求，其p99只是样本插值，无法支撑稳定的尾延迟判断。幂等重放测的是已提交结果复用，不能用50请求/秒代替新建能力。批量HTTP请求率与新增链接行率分别报告。

20账号25/s档全轮发送1996次、完成并确认1995次，数据库提交1996行；唯一未完成调用发生在停止时。只读对账确认全部1996个发送序号唯一提交，ID、路由、分表和各租户额度一致。该档依然失败，不把数据库提交补记为HTTP成功。

100条0.5批/秒档Outbox采样峰值2248，停止输入后约95秒排空。500条首档采样峰值2408，约115秒排空。20账号25/s档采样峰值2052，约85秒排空。以上排空时间按第一个停止后样本到第一个排空样本计算，约有5秒采样粒度；不能视为精确的最后请求到排空耗时。均未出现新增Outbox终态失败，批量返回内容与落库全量核对通过。

汇总表“Correct/sent %”使用正确数/实际发送数，未完成调用仍在分母内；k6原生自定义 `correct_rate` 使用正确数/已完成数。单账号25/s测量段分别为1243/1247=99.679%与1243/1244=99.920%，原始字段均保留；完整性、停止门槛和对账条件使该档继续判为失败，不因某个比例达标而放行。没有完成测量响应的阶段，汇总延迟保留N/A，不采用k6空样本默认的0ms。

A2/A3结束后，数据库共有25317条路由及链接，唯一ID与短码数量均一致，全部短码长度9；16分表与路由不存在缺失或错配，各租户额度核对通过。Outbox与metadata均排空，Outbox终态失败为0；metadata的25317次首次拒绝均属于预设目标路径。

## 4. 号段切换专项与未覆盖范围

顺序号段边界验证通过。该专项通过真实Admin同步批量接口创建，每批等待后台排空；不直接调用发号器，不修改数据库高水位，不重置号池。11批共新增4933行，含校准和对齐，耗时516.237秒，主要等待后台排空。专项只证明批量创建跨Current/Next号段的一致性，不作吞吐测量。

- 发送最终500条批次前，Current剩余250，已实测Next就绪；Current排他上界30001。
- 该批500个ID为29751–30250：旧段250、新段250，响应ID全部唯一；返回短码、路由、16分表和租户额度一致。切换后Current排他上界40001、剩余9750。
- 该500条批次的数据库领取次数增量为0，复用已预取Next；整个4933行专项仅增加1次数据库领取，保留10000个ID。没有补充失败、执行器拒绝或ID丢弃增量。
- 数据库保留上界不能当已发行数量：最终已发行30250，保留到40001排他上界。这里证明的是现有 `id-generator/SegmentIdGenerator` 双缓冲号段实现与真实批量创建链路的接线及一次切换，未覆盖多实例争抢、崩溃恢复或发号极限吞吐。

专项证据：`id-boundary-verification.json`。单次跨段HTTP耗时124.677ms，不当作p99或容量结论。

因上述停止条件，本轮没有进入A4修改/回收/恢复、正式跳转热集/大工作集/冷热对照、网关900/s基线、超额保护或关闭保护容量探索。也未执行三轮长时复测、30分钟稳态、JFR/CPU热点采样、正常网页metadata抓取、Agent、统计消费者、N-1及故障注入。现有CPU/GC为进程及Actuator采样；缓存命中/回源原因缺少完整细分指标，不能据此断言哈希或编码的CPU占比。

后续应先修复路由证明剩余预算与Outbox发布吞吐，补充对应单元/集成回归；限流key隔离风险先做真实竞争复现与回归。重新冻结制品和配置后，从契约冒烟开始复测，再进入高负载和长时确认。

## 5. 最终对账与环境收尾

最终数据库30250条路由/链接/唯一ID/唯一短码一致，各租户used额度总计30250，无预留、异步活动或校验任务残留；Outbox及metadata排空。30250个metadata任务均在首次受控拒绝路径终止，重试耗尽0、Outbox终态失败0。

从冻结基线11300行到最终边界新增18950行，Kafka route、metadata topic的offset增量各18950。click原始流增加57条，等于57次成功GET；请求原始流增加124条，等于93条REDIRECT结果＋31条EDGE结果。三路生产者attempted均等于delivered，failed/rejected增量均为0，metadata DLQ增量0。这里核对的是本次生产与持久化记录，不证明统计聚合消费者吞吐。Kafka保留的更早测试数据按基线offset排除。

证据：`final-boundary.json`、`final-ledger-verification.json`、`final-artifact-verification.json`、`environment-stop.json`。初始制品和源码清单保留，后续离线汇总及号段专项工具另存SHA；最终重新核对四个Java JAR与冻结值相同。无测量样本的汇总口径已用真实成功/预热停止产物验证；性能脚本Python语法检查及Git差异格式检查通过。Java业务制品未变，本轮未重复运行此前的Java单元/集成套件。

四个Java服务、k6及本轮APISIX已停止，本轮APISIX容器移除；四个测试依赖已停止并保留容器、数据库、Redis数据和Kafka日志。依赖CPU从测试用8–11改为本机全部逻辑CPU 0–15；Docker忽略空集合更新，因此保存的是显式0–15，不是最初的空字符串。该差异已写入环境停止记录。独立WSL随后终止，最终状态以 `environment-stop.json` 为准。未提交或推送代码。
