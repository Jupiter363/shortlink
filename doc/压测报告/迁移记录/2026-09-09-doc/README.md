# 2026-09-09 文档目录迁移记录

项目文档统一迁入 `doc/`：原 `docs/` 内容直接并入，原 `plan/` 迁至 `doc/plan/`，其中 19 份压测执行/分析报告放入 `doc/压测报告/过程报告/`，原压测归档迁至 `doc/压测报告/`。没有重新压测，所有结果与失败判定保留。

- [文件迁移清单](文件迁移清单.json)记录旧路径、旧 SHA256、新路径与迁移完成时的新 SHA256，以及链接调整情况。
- [迁移前归档清单](迁移前归档清单.json)和[迁移前校验表](迁移前SHA256SUMS.txt)保存原字节。旧归档中的 Markdown，以及旧归档引用的 23 份原稿，另存为 `迁移前文件/` 下的 `.snapshot` 字节快照，见[快照索引](迁移前快照索引.json)。快照里的相对路径是历史内容，不作为当前导航。
- [当前归档清单](../../2026-09-09/归档清单.json)保留原 `source` / `sourceSha256` 表示归档时的来源，并增加 `currentSource` / `currentSourceSha256` 表示迁移后的来源。当前 `sha256` 对应修正链接后的归档文件；迁移前 SHA 单独保留，避免覆盖历史证明。
- [当前归档校验表](../../2026-09-09/SHA256SUMS.txt)覆盖日期归档内除校验表自身之外的所有文件。本目录另有[迁移凭据校验表](SHA256SUMS.txt)。这些哈希用于完整性核对，并非数字签名。

38 份 `result.json`、专题 JSON、配置快照、测试凭据和图片均保持原字节。旧记录内的运行 ID、原工作区路径和冻结哈希继续表示当时的运行环境。大体积 native、JFR 和全量日志仍在原 `.work/`，没有搬入文档目录。

历史 `.work/report-archive-20260909/` 等生成、验收脚本保留原样，不能直接拿旧路径重跑新布局。核对当前归档时以当前清单及校验表为准。

迁移清单记录的是迁移完成时的工作区状态，并非整个 Git 提交的内容证明。尤其 `inboundDocuments` 中的 `deploy/apisix/README.md` 当时包含尚未提交的性能优化说明；文档目录整理提交仅纳入其中的路径更新，因此该项 `afterSha256` 不能用于核验此提交检出的 APISIX 说明文件。日期归档及迁移前快照仍按各自校验表核对。`doc/.gitattributes` 禁止自动换行转换，保证已归档文件在提交和检出时保留原字节。

[返回压测报告](../../README.md)。
