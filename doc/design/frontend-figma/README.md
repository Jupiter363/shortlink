# JUPITER RELAY · Figma 生成记录

生成日期：2026-09-14。关联前端重构 [Issue #27](https://github.com/Jupiter363/shortlink/issues/27)。

[打开 Figma 设计文件](https://www.figma.com/design/vY3F6K5aislCmSsE8hzfLC) · [短链接工作区](https://www.figma.com/design/vY3F6K5aislCmSsE8hzfLC?node-id=4-240)

使用已确认的「JUPITER RELAY / 木星中继站」主题，通过 Figma 插件批量创建原生文字、矢量、Auto Layout、Variables 和共享组件。当前插件没有接收自然语言提示词直接生成应用页面的专用接口，本轮按完整提示词用 Plugin API 构建设计对象。

业务依据：[页面与组件盘点](../../plan/前端重构/01-页面与组件盘点.md)、[完整生成提示词](../../plan/前端重构/02-Figma-AI低调用生成提示词.md)。

## 当前状态

**Figma 云端页面已生成，云端最终验收尚未完成。** 按后续“参照风格完善，不用插件”的要求，补全工作转到 [本地交互设计原型](../relay-preview/README.md)。本地版本独立补齐入口、控件、响应式导航和实际浏览器验收；它没有写回或替换 Figma 对象。

上一轮最后的只读结构审计和批量截图被 `figma_use_figma` 拒绝，返回：`You've reached the Figma MCP tool call limit on the Starter plan.` 随即停止远端调用，没有切换工具绕过额度。

Starter 的 Page 数量限制触发了主提示词预设的兼容布局：一个 Figma Page 下保留四个逻辑区域——产品合同、设计系统、业务页面、响应式与验收。部分业务主帧已提升到产品区域第一层，以稳定组织原型。

本轮记录了 24 次 Figma MCP 尝试：读取 8 次（含最后一次额度拒绝）、写入 11 次（6 次成功、5 次参数或事务失败）、独立截图 5 次。成功写入按共享基础、页面、流程、响应式和集中补全分批进行；这些次数不等同于实际账单金额。

## 已持久化的设计

| 页面 | 1440px 主帧 ID |
| --- | --- |
| P01 登录 | `4:9` |
| P02 注册 | `4:119` |
| P03 短链接工作区 | `4:240` |
| P04 回收站 | `4:641` |
| P05 访问统计 | `4:980` |
| P06 投放分析 Agent | `4:1153` |
| P07 风险中心 | `4:1315` |
| P08 安全风控 Agent | `4:1463` |
| P09 账户中心 | `4:1620` |

- 15 个响应式代表画板：1280px、768px、390px 各 5 个，重新排列内容；另有导航和分组抽屉。
- 12 条流程、75 个流程与状态帧、9 个辅助状态；主要流程建立了原型连接。
- 140 个 Primitive / Semantic 变量、7 种文字样式、3 种效果样式；按钮、输入框、状态、导航及原创双环标志和机器人资产。
- 集中补全新增 34 个反馈状态变体、32 个领域组件主件和产品合同文档。中文使用可用的 Noto Sans SC，标识符使用 Geist Mono。
- 统计、Agent 与风险保持真实业务边界：异步快照与未知数据明确区分，Agent 等待期间不模拟流式执行；人工审核、自动限流、停用已有策略分别呈现。

## 验证范围与剩余工作

已检查首轮 Foundations、Controls、P01、P03、P06 的实际截图，中文与图标可读，未见明显重叠。已读取生成结果确认九个主屏及十五个响应式画板的数量、名称与宽度。**这些检查不代表集中补全后的全部页面已经通过最终验收。**

以下仅记录 Figma 云端仍未写回的事项；本地版本对应完成情况与验证证据见上述原型文档。本轮没有继续调用远端插件：

1. 为响应式页面补连以下五类辅助入口。目标帧已经生成，尚未完成后续连接。

   | 入口目标 | 已生成帧 ID |
   | --- | --- |
   | 统计数据表 | `10:1819` |
   | 完整统计维度 | `10:1869` |
   | 记住登录选中态 | `10:1991` |
   | 投放分析调试详情 | `10:2038` |
   | 安全风控调试详情 | `10:2049` |

2. 写入响应式规则、Coverage Matrix、Prototype Index 和 Final QA，按实际检查结果标记覆盖情况。
3. 定点修复响应式导航：当前分组按钮应打开分组选择；关闭导航或分组抽屉应返回来源页面；共享导航抽屉不能固定选中短链接；1280px 折叠侧栏应补回收站入口。本地生成源码已修正，受额度限制尚未应用到已有 Figma 对象，禁止重跑生成器代替定点修复。
4. 检查集中补全后的所有主屏和代表性窄屏稿，重点核验注册长表单、统计维度、风险证据、手机端 Agent 回答的边界、行高和溢出。
5. 从真实原型入口核验五条核心流程及剩余 Storyboard 的连接，检查目的对象、数据口径和示例数值一致性。
6. 补齐部分独立控件源和变体，包括 Select、TextArea、DateTime、IconButton；补齐品牌标志的单色/小尺寸版本及机器人 Role/Expression 组件属性，不能将现有资产展示视为全部组件合同已完成。

本地执行记录、节点清单、生成脚本和已取得的截图位于 `.work/figma-relay/`。`07-pending-finish.js` 保存尚未执行的定点补连、导航修复和 QA 文档草稿；`06-pending-audit.js` 保存尚未执行的结构与截图审计。后续必须沿用当前文件和节点补齐，避免再次生成重复页面或组件。
