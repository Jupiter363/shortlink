# 开发与验证约定

## 最小必要测试与工作效率

- 后续默认只做与本次改动直接相关的最小必要测试，优先复用现有定向用例，不默认运行全模块、全仓库回归或压测。
- 只有出现测试失败、改动影响范围扩大或明确的未验证风险时，才扩大测试范围；说明扩大的具体原因。
- 已通过的验证不重复执行，除非相关代码再次修改或出现新的失败、风险。修复后只重跑受影响的用例。
- 纯文档、注释等低风险修改只检查对应内容和差异，不运行业务测试，不启动服务或 Docker；不为可逆、低影响修改新增价值有限的测试。
- 仓库或 CI 明确要求的必要检查仍须完成，但不额外叠加重复验证。
- 避免无关重构、反复审查、重复读取和不必要的并行任务。必要验证通过后及时交付，简要说明修改内容、验证结果和实际未覆盖范围。

## 已批准计划的持续推进

- 按用户已确认的实施计划，完成一项后自动进入下一项，不逐阶段等待“继续”。在已有授权范围内完成最小必要验证、提 PR 和合并；遇到需要用户决策或外部条件的真实阻断时再说明。
